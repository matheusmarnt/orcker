name: Docs

# Publish the VitePress documentation site (docs/) to GitHub Pages.
on:
  # Inherited from Yerd. The tag trigger is removed in the Orcker fork: the site
  # still describes Yerd and the Pages target is Yerd's, so an Orcker release tag
  # must not redeploy it. Manual dispatch stays for deliberate previews.
  workflow_dispatch:

# Least privilege for a Pages deploy via the GitHub Actions source.
permissions:
  contents: read
  pages: write
  id-token: write

# One live site at a time; never cancel an in-flight deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: build
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: docs
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: docs/package-lock.json

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - uses: actions/configure-pages@v5

      # path is repo-root relative (defaults.working-directory only affects `run`).
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    name: deploy
    needs: build
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
