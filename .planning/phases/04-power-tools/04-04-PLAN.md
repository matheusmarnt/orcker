---
phase: 04-power-tools
plan: "04"
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src-tauri/src/commands/database.rs
  - src-tauri/src/commands/projects.rs
  - src-tauri/src/lib.rs
  - src/ipc/bindings.ts
  - src/components/database/DatabaseTab.vue
  - src/components/projects/ProjectCard.vue
autonomous: true
requirements:
  - R-M6.1
  - R-M6.2
  - R-M6.3

must_haves:
  truths:
    - "When a project is registered or scaffolded, a {project_name}_testing database is silently auto-created in the global PostgreSQL container"
    - "User can dump the project database to a file via native save dialog"
    - "User can restore the project database from a file via native open dialog"
    - "User can open a psql CLI terminal in the project detail view"
  artifacts:
    - path: "src-tauri/src/commands/database.rs"
      provides: "create_testing_db, dump_db, restore_db, open_db_cli commands (fully implemented)"
      exports: ["create_testing_db", "dump_db", "restore_db", "open_db_cli"]
    - path: "src-tauri/src/commands/projects.rs"
      provides: "register_project and scaffold_project call create_testing_db_inner after success"
    - path: "src/components/database/DatabaseTab.vue"
      provides: "Database tab UI: testing DB status chip + Dump/Restore/CLI buttons"
  key_links:
    - from: "src-tauri/src/commands/projects.rs"
      to: "src-tauri/src/commands/database.rs"
      via: "call create_testing_db_inner() after register_project and scaffold_project succeed"
      pattern: "create_testing_db|testing"
      insertion_point: "In register_project: after the `Ok(project_id)` return path is reached (i.e. after the project is persisted and before the function returns), spawn a tokio::spawn fire-and-forget call. In scaffold_project: same pattern — after the scaffold completes successfully and before Ok(()) return."
    - from: "src/components/database/DatabaseTab.vue"
      to: "src-tauri/src/commands/database.rs"
      via: "typedError IPC: dump_db, restore_db, open_db_cli"
      pattern: "dump_db|restore_db|open_db_cli"
---

<objective>
Implement all M6 Database commands in Rust and the DatabaseTab Vue component. Auto-creates testing DB on project register/scaffold, provides dump/restore via native file picker, and opens a psql CLI terminal reusing the existing xterm.js streaming panel.

Purpose: R-M6.1/6.2/6.3 — database workflows scoped per-project within the project detail view.
Output: Fully implemented database.rs commands, DatabaseTab.vue with DB status chip + actions, auto-create hooked into register/scaffold.
</objective>

<execution_context>
@/home/matheusmariano/.claude/get-shit-done/workflows/execute-plan.md
@/home/matheusmariano/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-power-tools/04-RESEARCH.md
@.planning/phases/04-power-tools/04-01-SUMMARY.md
@src-tauri/src/commands/artisan.rs
@src-tauri/src/adapters/docker/exec.rs
@src/components/CommandPanel.vue
</context>

<interfaces>
<!-- From artisan.rs / exec.rs: docker_exec_stream adapter signature -->
<!-- docker_exec_stream(docker, container_id, cmd: Vec<String>, token: CancellationToken, channel: Channel<String>) -->
<!-- Global PostgreSQL container name convention: "orcker-global-postgres-1" (or detected via label) -->
<!-- tauri-plugin-dialog: already installed; use dialog::open() for file open, dialog::save() for file save -->
<!-- AppError variants: Internal (fs/exec errors), NotFound (container not found) -->

<!-- projects.rs hook insertion points:
     register_project: after the final `Ok(project_id)` line (search for "Ok(project_id)" — there is only one return path
     that signifies success). Insert before return:
       let docker_clone = docker.clone();
       let name_clone = project_name.clone();
       tokio::spawn(async move {
           let _ = create_testing_db_inner(&docker_clone, &name_clone).await;
       });
     scaffold_project: same pattern — search for `Ok(())` at the end of the success branch,
     insert tokio::spawn before the return. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement database Rust commands</name>
  <files>
    src-tauri/src/commands/database.rs,
    src-tauri/src/commands/projects.rs,
    src-tauri/src/lib.rs,
    src/ipc/bindings.ts
  </files>
  <behavior>
    - create_testing_db: execs `psql -U postgres -c "CREATE DATABASE {name}_testing"` in the global postgres container; ignores "already exists" error (idempotent)
    - dump_db: opens native save dialog → shells out `docker exec -t {container} pg_dump -U postgres {db}` piped to chosen file path
    - restore_db: opens native open dialog → pipes chosen file to `docker exec -i {container} psql -U postgres {db}`
    - open_db_cli: spawns `docker exec -it {container} psql -U postgres {db}` via existing docker_exec_stream adapter; returns Channel<String> stream for xterm.js
  </behavior>
  <action>
Replace the stub body in `src-tauri/src/commands/database.rs` with full implementations:

**create_testing_db** — internal helper (not a Tauri command) called from register_project and scaffold_project:
```rust
pub async fn create_testing_db_inner(
    docker: &bollard::Docker,
    project_name: &str,
) -> Result<(), AppError> {
    // Find global postgres container by label "com.docker.compose.service=postgres"
    // and "com.docker.compose.project=orcker-global"
    let db_name = format!("{}_testing", project_name.replace('-', "_").replace(' ', "_"));
    let cmd = vec![
        "psql".into(), "-U".into(), "postgres".into(),
        "-c".into(), format!("CREATE DATABASE \"{db_name}\""),
    ];
    // Run docker exec (one-shot, not streaming) using bollard exec API directly
    // If output contains "already exists", treat as Ok(())
    // Emit Tauri event "database://testing-created" with db_name on success
    Ok(()) // implement inline using bollard exec
}
```

Expose as `#[tauri::command] pub async fn create_testing_db(...)` that calls the inner function.

**dump_db** — use `tauri_plugin_dialog::DialogExt` to open a save dialog with `.add_filter("SQL", &["sql"])`, then shell out `std::process::Command` (blocking, run in `tauri::async_runtime::spawn_blocking`) for `docker exec -t {container} pg_dump -U postgres {db} > {path}`.

**restore_db** — open dialog with `.add_filter("SQL", &["sql"])`, pipe file content to `docker exec -i {container} psql -U postgres {db}`.

**open_db_cli** — delegate to existing `docker_exec_stream` adapter with `cmd = vec!["psql", "-U", "postgres", db_name]`. Register this as a streaming command returning `Channel<String>`.

Register all four commands in `lib.rs` (replacing the previous stub registration notice — now they are implemented). Regenerate bindings. Run `cargo fmt`.

**Hook into `projects.rs`**: after `register_project` succeeds (immediately before the `Ok(project_id)` return), and after `scaffold_project` completes successfully (immediately before the `Ok(())` return), call `create_testing_db_inner` in a `tokio::spawn` fire-and-forget — failure emits an error event but does NOT fail the project operation:
```rust
let docker_clone = docker.clone();
let name_clone = project_name.clone();
tokio::spawn(async move {
    let _ = create_testing_db_inner(&docker_clone, &name_clone).await;
});
```
  </action>
  <verify>
    <automated>cd /home/matheusmariano/personal-projects/project-orcker/orcker/src-tauri && cargo test -- database 2>&1 | tail -10</automated>
  </verify>
  <done>cargo test database passes (including the previously-ignored stub test now filled in); cargo clippy clean; all four commands registered in lib.rs; create_testing_db_inner called from register_project and scaffold_project</done>
</task>

<task type="auto">
  <name>Task 2: DatabaseTab Vue component</name>
  <files>
    src/components/database/DatabaseTab.vue,
    src/components/projects/ProjectCard.vue
  </files>
  <action>
Create `src/components/database/DatabaseTab.vue`:

- Props: `projectId: string`, `projectName: string`
- Shows a status chip: "Testing DB: {projectName}_testing" with a green dot if it was auto-created (derive from a local ref `testingDbExists`); a "Create" button if not yet created (calls create_testing_db IPC)
- Three action buttons: "Dump" (calls dump_db IPC), "Restore" (calls restore_db IPC), "Open CLI" (calls open_db_cli IPC → opens CommandPanel with the streaming channel)
- Reuse `<CommandPanel>` component (from Phase 3, `src/components/projects/CommandPanel.vue`) for CLI output — pass the channel from open_db_cli
- All IPC calls wrapped in `typedError(...)` with `.catch()` handlers showing Sonner error toasts
- Loading state per button (use individual `ref isLoadingDump`, `isLoadingRestore`, `isOpeningCli`)

In `src/components/projects/ProjectCard.vue`:
- Add a "Database" tab within the project card/detail using the existing tab pattern (if tabs exist) or add a "Database" button that toggles `showDatabaseTab` ref
- Render `<DatabaseTab v-if="showDatabaseTab" :project-id="project.id" :project-name="project.name" />`

Use `typedError` from `@/ipc/bindings`. Follow the existing CommandPanel mounting pattern (v-if, not v-show, so Vue teardown clears channel listeners automatically).
  </action>
  <verify>
    <automated>cd /home/matheusmariano/personal-projects/project-orcker/orcker && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <done>pnpm type-check clean; DatabaseTab.vue exists with status chip + Dump/Restore/CLI buttons; ProjectCard has Database tab toggle; all IPC calls use typedError with catch</done>
</task>

</tasks>

<verification>
- `cargo clippy -- -D warnings` exits 0
- `cargo test -- database` passes
- `pnpm type-check` exits 0
- `cargo fmt --check` exits 0
</verification>

<success_criteria>
create_testing_db_inner called from register_project + scaffold_project (fire-and-forget, before Ok return); dump_db + restore_db use native file picker dialogs; open_db_cli reuses docker_exec_stream; DatabaseTab.vue has testing DB chip + 3 action buttons; all IPC uses typedError with catch.
</success_criteria>

<output>
After completion, create `.planning/phases/04-power-tools/04-04-SUMMARY.md`
</output>
