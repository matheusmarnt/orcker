# Cycle log — SPEC-0048

Spec: `specs/SPEC-0048-pure-crate-coverage-metric.md`
Branch: `feat/SPEC-0048-pure-crate-coverage-metric`
Surface: `.github/`, `docs/`

## S1 — context

`specs/TRACEABILITY.md`'s `Pure-crate coverage` column (added by SPEC-0047) has read
`—` on every cycle since: nothing in the repo measures it, and `cargo-llvm-cov` /
`llvm-tools` are not installed anywhere (`specs/DECISIONS.md`'s SPEC-0004 entry). This
spec puts the measurement in CI, report-only, so the column can finally carry a number.

Spec front matter at `HEAD` (`git show "HEAD:specs/SPEC-0048-*.md" | awk -F': '
'/^status:/{print $2; exit}'`): `approved`. `depends_on: [SPEC-0047]`, which is
`accepted`. Selectable.

The spec reached `approved` twice: first with only `Context` (missing `Requirements`
through `Agent notes` that SDD section 4 requires — the same gap SPEC-0049 was
ESCALATEd for in round 1), caught before any implementation started. Fixed by the
human's own decision, out of band: the `spec-writer` subagent drafted the missing
sections from the already-approved Context (deciding nothing new), committed with
`status: draft` (`b404f9d..c97f0c1`), then the human re-approved the completed body
(`c97f0c1..4d1579b`, status-line + `ROADMAP.md` row only). `/spec-next` re-selected the
spec from that second approval. The branch itself was re-pointed at `main` rather than
reused, since its one prior commit (an `approved -> in_progress` flip against the
incomplete body) was superseded content, confirmed unpublished first (`git cherry main
feat/SPEC-0048-pure-crate-coverage-metric` printed exactly that one `+` commit).

## S2 — plan

No Rust changes; no crate instruction file applies. Two files:

1. `.github/workflows/ci.yml`: one step appended to the `rust` job (after the
   pacman/rpm mutual-exclusion assert, before the `frontend` job), scoped
   `if: matrix.os == 'ubuntu-22.04'` (the single x86_64 Linux leg — not
   `ubuntu-22.04-arm`, not `macos-14`, so the number is reported once) and
   `continue-on-error: true` (report-only, mirrors the existing convention at
   `release.yml:341`/`:398`): `rustup component add llvm-tools-preview`,
   `cargo install cargo-llvm-cov --locked`, then `cargo llvm-cov --summary-only -p
   orcker-core -p orcker-stack -p orcker-engine`. `orcker-catalog` is not in the `-p`
   list — `ls crates/` confirms the crate does not exist yet.
2. `docs/SDD.md` section 11: one new English sentence appended to the existing
   (Portuguese) metrics paragraph, per root `CLAUDE.md`'s American-English mandate for
   every shared record including `docs/`, stating where the number comes from and that
   a failed/skipped run leaves the column `—` at S8 instead of blocking it.

Gate impact: no `.rs` file in the diff, so `scripts/clippy-allow-baseline.txt` is
untouched; gate step 6 (surface) is the only one this spec can fail, and both touched
paths are declared.

## S3 — RED evidence

Taken at `HEAD`, before either edit.

```
$ grep -c 'llvm-cov' .github/workflows/ci.yml
0

$ cargo llvm-cov --version
error: no such command: `llvm-cov`
```

Nothing measures pure-crate coverage. No unit/integration test is possible for a
CI-workflow-only change (no trait, no fake, no side effect behind a boundary); the E2E
substitute is running the exact command the new CI step will run, locally, since
triggering the actual GitHub Actions workflow needs a push and this repository reserves
`git push` for the human. Installed `llvm-tools-preview` (`rustup component add
llvm-tools-preview`) and `cargo-llvm-cov 0.9.0` (`cargo install cargo-llvm-cov --locked`)
to make that substitute real rather than assumed:

```
$ cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine
...
TOTAL   7631   339   95.56%   594   30   94.95%   4692   192   95.91%   0   0   -
```

(columns: region/miss/cover%, function/miss/cover%, line/miss/cover%, branch/miss/cover%
— `cargo-llvm-cov` reports no branch data, hence the trailing `0 0 -`.) This is the
exact command AC1 and the new CI step both name; it produces a real number over exactly
the three named pure crates, proving the command itself is correct before it goes into
YAML.

## S4 — implementation

Two edits, exactly as planned in S2:

- `.github/workflows/ci.yml`: new step `Pure-crate coverage (report only)`, `if:
  matrix.os == 'ubuntu-22.04'`, `continue-on-error: true`, running the three commands
  above. A comment above it states why it never gates and why it runs on one leg only.
- `docs/SDD.md` section 11: new sentence appended, in English, after the existing
  Portuguese paragraph; that paragraph itself is unedited.

No other file touched. `scripts/gate.sh` is untouched (R2).

## S5 — GREEN evidence

```
$ grep -n "llvm-cov\|matrix.os == 'ubuntu-22.04'\|continue-on-error: true" .github/workflows/ci.yml   # AC2
114:        if: matrix.os == 'ubuntu-22.04'
115:        continue-on-error: true
118:          cargo install cargo-llvm-cov --locked
119:          cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine

$ git diff --name-only main~1..HEAD -- scripts/gate.sh                                                # AC3
(empty)

$ grep -n "llvm-cov" docs/SDD.md                                                                      # AC4
440:The `Pure-crate coverage` number comes from the `rust (ubuntu-22.04)` CI job's
`Pure-crate coverage (report only)` step (`cargo llvm-cov --summary-only` over the pure
crates), which is report-only (`continue-on-error: true`) and deliberately absent from
`scripts/gate.sh`. A failed or skipped run leaves the column `—` at S8 rather than
blocking the cycle.

$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML OK')"
YAML OK
```

AC1 (the command itself works over the three named crates, real `TOTAL` line) already
shown in S3 — the step's own content, not the workflow trigger, is what changed, so the
same run stands as its GREEN evidence too; nothing about the command changed between S3
and S4, only its new home in `ci.yml`.

Gate:

```
$ scripts/gate.sh specs/SPEC-0048-pure-crate-coverage-metric.md
...
[gate 6/6] surface check
[surface] allowed prefixes: specs/ docs/ scripts/clippy-allow-baseline.txt .github/ docs/
[gate] OK
```

No `Work found and not done` this cycle.

## S6 — self-review

| AC | Verdict | Evidence |
|----|---------|----------|
| AC1 | pass | local `cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine` run, real `TOTAL` line (S3/S5) |
| AC2 | pass | `grep -n` shows the step scoped to `matrix.os == 'ubuntu-22.04'` and `continue-on-error: true` |
| AC3 | pass | `git diff --name-only main~1..HEAD -- scripts/gate.sh` empty |
| AC4 | pass | `grep -n "llvm-cov" docs/SDD.md` shows the new English sentence |
| AC5 | pass | `scripts/gate.sh specs/SPEC-0048-pure-crate-coverage-metric.md` -> `[gate] OK` |

R1-R3 hold by AC1-AC5: the step exists, is scoped correctly, never gates; `gate.sh` is
untouched; `docs/SDD.md` states the source and the `—`-on-failure/skip behavior.

**Weakest point, stated plainly.** AC1's evidence is a local run, not the workflow
actually executing on GitHub's Linux runner — the workflow itself cannot be triggered
from this session (push is human-only). The YAML is syntax-checked (`python3 -c
"import yaml"`) and hand-verified against the repo's own existing `continue-on-error`
and matrix-scoping conventions, but the first real signal that the step runs green (or
degrades gracefully to `—`) on GitHub's hosted runner comes from the next push, not from
this cycle.

## S7 — supervisor verdict (round 1)

Everything below was re-derived by the supervisor in a clean context, not read off S1-S6.

**Independently reproduced.**

- `git log --oneline main..HEAD` -> exactly one commit, `f6cc9b8 docs(specs): start SPEC-0048
  pure-crate coverage metric`, `--stat` = the spec's status line only (1 insertion, 1
  deletion). The implementation is staged, not committed; `git diff --cached --name-only`
  = `.github/workflows/ci.yml`, `docs/SDD.md`. Nothing unstaged. The only untracked path is
  this cycle log.
- DT9: the approval sequence is on `main`, ahead of the branch's `in_progress` commit.
  `2f2c713 docs(specs): approve SPEC-0048 pure-crate coverage metric` touches exactly
  `specs/ROADMAP.md` (row 48 `draft -> approved`) and the spec's `status:` line, 1 insertion
  and 1 deletion each, authored by the human. The earlier `0d18755 docs(specs): draft
  SPEC-0048 body sections` (105 insertions, status kept `draft`) and `2fca75a` (the first,
  Context-only approval) are the split S1 describes; the operative ratification of the
  complete body is `2f2c713`, and it is clean.
- R2 / AC3 verified by content, not by a name filter: `git rev-parse main:scripts/gate.sh`
  and `git hash-object scripts/gate.sh` both print `89785026f1a2d5f2bbd12a3139ca5e112179686a`.
  The working tree's `scripts/gate.sh` is byte-identical to `main`. `git diff main..HEAD --
  scripts/`, `git diff --cached -- scripts/` and `git diff HEAD -- scripts/` are all empty.
- `orcker-catalog` genuinely does not exist: `ls crates/` lists 17 crates, `orcker-core`,
  `orcker-stack` and `orcker-engine` among them, no `orcker-catalog`. The `-p` list is
  correct as shipped, not an oversight, and the spec's Out of scope already says so.
- R1 / AC2 verified structurally rather than by grep: parsing the workflow with
  `yaml.safe_load` and selecting from `jobs.rust.steps` yields
  `{'name': 'Pure-crate coverage (report only)', 'if': "matrix.os == 'ubuntu-22.04'",
  'continue-on-error': True, 'run': 'rustup component add llvm-tools-preview\ncargo install
  cargo-llvm-cov --locked\ncargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p
  orcker-engine\n'}`. The step is inside the `rust` job, the scoping value matches the
  matrix (`os: [ubuntu-22.04, ubuntu-22.04-arm, macos-14]`), so it runs on the single
  x86_64 Linux leg, and `continue-on-error` is a real boolean, not a string.
- R3 / AC4: `docs/SDD.md:440` carries the new text, inside section 11, after the existing
  Portuguese metrics paragraph and before `## 12`. In English per root `CLAUDE.md`, and it
  states both required facts (where the number comes from; `—` on failure or skip instead
  of blocking S8). Two sentences rather than the literal "one sentence" of R3 — content
  requirement met, form deviation immaterial, no finding.
- AC1 re-run by the supervisor, not accepted from the paste:
  `cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine` exits 0 and
  ends `TOTAL 7631 339 95.56% | 594 30 94.95% | 4692 192 95.91% | 0 0 -`, matching S3's
  numbers digit for digit. The metric is real and above the SDD section 11 target of >= 80%.
- AC5 / DT1: `scripts/gate.sh specs/SPEC-0048-pure-crate-coverage-metric.md` re-run by the
  supervisor, `EXIT=0`, ending `[gate 6/6] surface check` / `[gate] OK`. `scripts/surface-check.sh`
  run standalone also exits 0.
- DT4 sampled rather than trusted: `git show main:.github/workflows/ci.yml | grep -c llvm-cov`
  prints `0`, confirming S3's RED baseline. The second half of S3's RED
  (`cargo llvm-cov --version` -> `no such command`) is no longer reproducible now that
  `cargo-llvm-cov 0.9.0` is installed, which is expected and disclosed.
- DT5/DT6/DT7: the diff is two files. No `crates/orcker-ipc` path, no `Cargo.toml`,
  `Cargo.lock` or `package.json`, no test file, no `[workspace.lints]`, no `scripts/`.
  The CI-only `cargo-llvm-cov` + `llvm-tools-preview` tooling is declared in the spec's
  Design & contracts, which is what DT6 asks for; the shipped `--locked` on the install is a
  hardening detail over the spec's bare `cargo install`, accepted.
- DT8: no Rust changed, and gate step 2 (`clippy -D warnings`) is green.
- JG1: the diff is exactly R1 and R3, with R2 discharged by absence. No extra file, no extra
  step, no threshold, no other CI leg. JG2-JG4 are vacuous (no Rust). JG6: the six-line
  comment above the step matches the file's own convention (the pacman/rpm/mutual-exclusion
  steps each carry the same kind of why-block).
- JG5 read honestly: AC2's evidence is a grep of the file the cycle edited, which is
  tautological in the general case. It is accepted here because the spec's approved Test
  plan states the reason (the step cannot execute without a `git push`, reserved for the
  human) and because the discriminating half of the evidence — AC1, the command itself over
  the three real crates — was reproduced independently and is not self-referential. The
  supervisor strengthened AC2 beyond the spec's grep by parsing the YAML and asserting the
  step's position and value types.
- JG8: `specs/TRACEABILITY.md` has no SPEC-0048 row yet and the spec is still `in_progress`.
  That is correct at S7 — SDD section 6's S8 node and the TRACEABILITY footer both place the
  row and the status flip at S8. Carried as a release condition below, not a finding.

**Release conditions for S8** (none blocking; all are S8's normal work):

1. Flip the spec's `status:` to `accepted` and `specs/ROADMAP.md` row 48 to `accepted`.
2. Add the SPEC-0048 row to `specs/TRACEABILITY.md` with `Attempts: 0`, `Escalates: 0`,
   `Final verdict: APPROVE (round 1)`.
3. This is the first cycle able to fill its own `Pure-crate coverage` cell: record
   **95.91% line / 95.56% region** over `orcker-core` + `orcker-stack` + `orcker-engine`,
   and say it came from a local `cargo llvm-cov` run rather than the CI step, since the step
   has not executed on a hosted runner yet.
4. Commit the staged `.github/workflows/ci.yml` + `docs/SDD.md` and this log as one atomic
   commit, Conventional Commits with a `ci` scope, referencing SPEC-0048 and FR-001, in
   American English, with no AI-attribution trailer of any kind.
5. No `specs/DECISIONS.md` entry is required: the one deviation a reader might look for
   (`orcker-catalog` absent from the `-p` list) is already stated in the spec's R1 and Out
   of scope, so it is spec text, not a deviation from it.

```yaml
spec: SPEC-0048
verdict: APPROVE
deterministic:
  DT1_gate: pass
  DT2_surface: pass
  DT3_ac_mapping: pass
  DT4_red_evidence: pass
  DT5_ipc_stability: pass
  DT6_deps: pass
  DT7_gate_integrity: pass
  DT8_public_docs: pass
  DT9_approval_commit: pass
acceptance:
  AC1: { status: pass, evidence: "supervisor re-ran `cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine`, exit 0, TOTAL 7631 339 95.56% / 594 30 94.95% / 4692 192 95.91%" }
  AC2: { status: pass, evidence: "yaml.safe_load of .github/workflows/ci.yml: jobs.rust.steps contains 'Pure-crate coverage (report only)' with if: matrix.os == 'ubuntu-22.04' and continue-on-error: true" }
  AC3: { status: pass, evidence: "git rev-parse main:scripts/gate.sh == git hash-object scripts/gate.sh == 89785026f1a2d5f2bbd12a3139ca5e112179686a; git diff main..HEAD -- scripts/ empty" }
  AC4: { status: pass, evidence: "docs/SDD.md:440, English, in section 11: names the rust (ubuntu-22.04) llvm-cov step as the source and the `—`-instead-of-blocking rule" }
  AC5: { status: pass, evidence: "supervisor ran scripts/gate.sh specs/SPEC-0048-pure-crate-coverage-metric.md -> EXIT=0, [gate] OK (6/6)" }
judgment_findings: []
regression_notes: "Two named risks, neither blocking. (1) The CI step has never executed on a GitHub-hosted runner: `git push` is human-only, so its first real signal is the next push. Failure modes are contained by `continue-on-error: true` plus the single-leg `if:` — a broken step degrades the column to `—`, which docs/SDD.md:440 now sanctions, and cannot fail the `rust` job or block S8. (2) The ubuntu-22.04 leg gets longer and its Swatinem/rust-cache entry larger: `cargo install cargo-llvm-cov --locked` builds from source and the coverage run rebuilds the workspace instrumented into target/llvm-cov-target. Wall-clock only, on one of three legs, with no effect on the gate or on the other two legs. Untouched-but-affected area: none in Rust — the diff contains no code, and `cargo test --workspace` is green in the supervisor's own gate run."
escalate_reason: null
```
