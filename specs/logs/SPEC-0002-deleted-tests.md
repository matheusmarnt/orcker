# SPEC-0002 — deleted-test review list

Mechanically derived: every `#[test]`/`#[tokio::test]` fn present at `HEAD` and
absent from the SPEC-0002 diff, **restricted to files that still exist**.

- 671 test fns deleted in total
- 445 died with their file (crate/module removed by R1/R2 - authorized)
- 90 in `wire_stability.rs` (R4-authorized reset, verified byte-identical)
- **136 deleted from surviving files** - the entire DT7 surface

Reproduce with `/tmp/enum_tests.py` (see cycle log S7 attempt 3).

| status | file | test | note |
| --- | --- | --- | --- |
| drop | `apps/orcker-gui/src-tauri/src/tray.rs` | `default_php_choices_drops_legacy_versions` | subject `default_php_choices` deleted by R5; unrestorable |
| drop | `apps/orcker-gui/src-tauri/src/tray.rs` | `default_php_choices_is_empty_when_only_legacy_is_installed` | subject `default_php_choices` deleted by R5; unrestorable |
| drop | `bin/orcker/src/cli.rs` | `exec_captures_hyphenated_tool_args` | subject `Command::Exec` + `ExecTool`, deleted by SPEC-0002; 0 refs at HEAD (the `Exec`/`Which` hits in a tree grep are the English word in doc comments); asserts `ExecTool::Php` and exec's trailing-arg capture |
| drop | `bin/orcker/src/cli.rs` | `exec_forwards_a_trailing_json_flag_to_the_tool` | subject `Command::Exec` + `ExecTool`, deleted by SPEC-0002; 0 refs at HEAD (the `Exec`/`Which` hits in a tree grep are the English word in doc comments); asserts exec's `trailing_var_arg` capture, which has no surviving subcommand (0 hits for `trailing_var_arg` in bin/orcker/src outside the stale comment) |
| drop | `bin/orcker/src/cli.rs` | `exec_rejects_an_unknown_tool` | subject `Command::Exec` + `ExecTool`, deleted by SPEC-0002; 0 refs at HEAD (the `Exec`/`Which` hits in a tree grep are the English word in doc comments); SPEC-0002 already reasoned this one: it now passes for the wrong reason, clap rejecting an unknown subcommand rather than the tool argument |
| drop | `bin/orcker/src/cli.rs` | `exec_takes_orckers_json_flag_before_the_tool` | subject `Command::Exec` + `ExecTool`, deleted by SPEC-0002; 0 refs at HEAD (the `Exec`/`Which` hits in a tree grep are the English word in doc comments); the global `--json` survives, but the assertion is only meaningful against exec's trailing-arg capture - with `Exec` gone the pre-subcommand position parses trivially and the test would no longer pin `global = true` |
| drop | `bin/orcker/src/cli.rs` | `exec_takes_site_before_the_tool` | subject `Command::Exec` + `ExecTool`, deleted by SPEC-0002; 0 refs at HEAD (the `Exec`/`Which` hits in a tree grep are the English word in doc comments); asserts `ExecTool::Composer` and exec's `--site` |
| drop | `bin/orcker/src/cli.rs` | `which_parses_php_with_an_optional_site` | subject `Command::Which` + `WhichTool`, deleted by SPEC-0002; 0 refs at HEAD; asserts `WhichTool::Php` and which's `--site` |
| drop | `bin/orcker/src/cli.rs` | `which_rejects_composer` | subject `Command::Which` + `WhichTool`, deleted by SPEC-0002; 0 refs at HEAD; same wrong-reason tautology as `exec_rejects_an_unknown_tool` |
| drop | `bin/orcker/src/lib.rs` | `canonicalize_db_paths_backup_absolute_is_unchanged` | subject `canonicalize_db_paths` itself - the fn is gone from lib.rs (only a stale doc ref at :786 and an empty section banner at :970 remain), along with `Request::BackupDatabase`/`RestoreDatabase` |
| drop | `bin/orcker/src/lib.rs` | `canonicalize_db_paths_backup_relative_is_absolutised` | subject `canonicalize_db_paths` itself - the fn is gone from lib.rs (only a stale doc ref at :786 and an empty section banner at :970 remain), along with `Request::BackupDatabase`/`RestoreDatabase` |
| drop | `bin/orcker/src/lib.rs` | `canonicalize_db_paths_other_request_passes_through` | subject `canonicalize_db_paths` itself - the fn is gone from lib.rs (only a stale doc ref at :786 and an empty section banner at :970 remain), along with `Request::BackupDatabase`/`RestoreDatabase`. Its `Request::Ping` pass-through assertion is the one that still compiles, but the subject is the deleted fn, so there is no surviving symbol to retarget onto; the sibling `canonicalize_unpark_passes_through_other_requests` already pins that shape for a fn that does survive |
| drop | `bin/orcker/src/lib.rs` | `canonicalize_db_paths_restore_existing_file_is_canonicalised` | subject `canonicalize_db_paths` itself - the fn is gone from lib.rs (only a stale doc ref at :786 and an empty section banner at :970 remain), along with `Request::BackupDatabase`/`RestoreDatabase` |
| drop | `bin/orcker/src/lib.rs` | `canonicalize_db_paths_restore_missing_file_is_usage_error` | subject `canonicalize_db_paths` itself - the fn is gone from lib.rs (only a stale doc ref at :786 and an empty section banner at :970 remain), along with `Request::BackupDatabase`/`RestoreDatabase` |
| drop | `bin/orcker/src/lib.rs` | `first_php_on_path_returns_option` | subject `first_php_on_path`, 0 refs at HEAD |
| drop | `bin/orcker/src/lib.rs` | `print_php_path_hint_runs` | subject `print_php_path_hint`, 0 refs at HEAD |
| drop | `bin/orcker/src/map.rs` | `install_legacy_requires_flag` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| drop | `bin/orcker/src/map.rs` | `install_php_rejects_bad_version` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| scrub | `bin/orcker/src/map.rs` | `maps_each_command_to_its_request` | restored verbatim first; the compiler named 53 dead assertions, all PHP-family. Kept the 17 surviving arms (Ping, Sites, Park, Unlink, Unpark, List{Parked}, Tools, Install/Uninstall{Tool}, Restart{Daemon}, Secure, Unsecure, FrontController x2, Root x3) - `to_request(&Command::` appears **zero** times for any of them at HEAD. Also re-added the `use std::path::PathBuf` an import-pruner removed (R1). Its two Update->CheckUpdate arms were dropped as duplicates of `bare_update_stable_flag_overrides_channel` + `channel_from_flags_table` |
| drop | `bin/orcker/src/map.rs` | `maps_every_db_action` | subject `DbAction` + `Request::ListDatabases`/`CreateDatabase`/`DropDatabase`/`BackupDatabase`/`RestoreDatabase` and `Response::Databases`, all absent |
| drop | `bin/orcker/src/map.rs` | `maps_every_service_action` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `maps_service_set_and_unset_to_a_single_entry_override_map` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `maps_services_command` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `php_ext_add_maps_and_defaults_name` | subject `PhpExtAction` + `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, all absent |
| drop | `bin/orcker/src/map.rs` | `php_ext_add_rejects_non_absolute_path_client_side` | subject `PhpExtAction` + `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, all absent |
| drop | `bin/orcker/src/map.rs` | `php_ext_list_and_remove_map` | subject `PhpExtAction` + `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, all absent |
| drop | `bin/orcker/src/map.rs` | `php_ini_actions_map_and_validate` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| drop | `bin/orcker/src/map.rs` | `php_pool_actions_map_and_validate` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| scrub | `bin/orcker/src/map.rs` | `rejects_bad_version_and_name_before_connect` | kept the 2 site-name arms (`Unlink` "bad/name", `Secure` "bad name"), both uncovered; dropped the 5 that drove `Command::Use` and `SetTarget::Php`. Restored as `rejects_a_bad_site_name_before_connect` since it no longer checks versions |
| drop | `bin/orcker/src/map.rs` | `renders_available_php_legacy_section` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_available_php_tagging_installed` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_available_services` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `renders_databases` | subject `DbAction` + `Request::ListDatabases`/`CreateDatabase`/`DropDatabase`/`BackupDatabase`/`RestoreDatabase` and `Response::Databases`, all absent |
| drop | `bin/orcker/src/map.rs` | `renders_empty_php_extensions` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| restore | `bin/orcker/src/map.rs` | `renders_human_responses_and_exit_codes` | **restored verbatim, zero edits.** Every type it touches survives unchanged: `Response::Pong/Ok/Sites/Tools/Error`, `ToolStatus`, `SiteEntry` (incl. `uses_front_controller`), `ErrorCode::NotFound`. This is the renderer SPEC-0002's supervisor named as its own AC4 evidence; no surviving test covers human rendering of Pong/Ok/Sites/Tools/Error |
| drop | `bin/orcker/src/map.rs` | `renders_php_extensions_grouped_with_missing_flag` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_php_settings_block` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_php_versions_marking_default` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_php_versions_with_per_version_overrides_and_directives` | subject is a `Response` variant deleted with the native runtime - it cannot be constructed at HEAD (subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs) |
| drop | `bin/orcker/src/map.rs` | `renders_service_logs` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `renders_service_overrides` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `renders_services_table_and_states` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `service_set_refuses_a_bad_shape_and_a_service_without_overrides` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `service_set_refuses_a_reserved_key_in_either_spelling` | subject `ServiceAction` + the `Request::*Service*` variants and `Response::Services`/`AvailableServices`/`ServiceOverrides`/`ServiceLogs`, all absent |
| drop | `bin/orcker/src/map.rs` | `unset_unknown_php_setting_is_usage_error` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| drop | `bin/orcker/src/map.rs` | `update_php_with_self_update_flags_each_error` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| drop | `bin/orcker/src/map.rs` | `use_rejects_legacy_default` | subject is the PHP command surface: `Command::Use`/`Set`/`Unset`, `InstallTarget::Php`, `RestartTarget::Php`, `UninstallTarget::Php`, `ListTarget::Php`, `UpdateTarget::Php` and the `Request::*Php*` variants - every one absent from cli.rs and request.rs |
| drop | `bin/orcker/tests/cli_e2e.rs` | `php_version_config_round_trips_against_daemon` | subject is the per-version PHP config surface: `Command::Php` (`PhpAction::Ini`/`Pool`), `SetTarget::Php`, `UnsetTarget::Php`, `Response::PhpVersions` and `daemon.php_manager` - all deleted, 0 refs in tree. Its one generic assertion (config round-trips to `orcker.toml`) is already pinned by the surviving `cli_commands_round_trip_against_daemon` (cli_e2e.rs:306) |
| retarget | `bin/orckerd/src/backend_resolver.rs` | `explicit_override_beats_the_detected_default` | override semantics themselves are already pinned pure by `uses_front_controller_default_and_override` (orcker-core site.rs:675); what was uncovered is the override surviving the daemon's derivation into the wire entry. Rows 4-5 of `list_sites_derives_front_controller_from_the_wordpress_registry` (ipc_server.rs) |
| retarget | `bin/orckerd/src/backend_resolver.rs` | `subdir_framework_funnels_but_root_served_executes_directly` | vehicle `DaemonBackendResolver::allows_direct_script_execution` died with the FPM pool; the behaviour did not. Substituted onto the surviving derivation at ipc_server.rs:109 -> `SiteEntry::uses_front_controller`. Rows 1-2 of `list_sites_derives_front_controller_from_the_wordpress_registry` (ipc_server.rs) |
| retarget | `bin/orckerd/src/backend_resolver.rs` | `wordpress_in_subdir_still_executes_directly` | same substitution; this is the only assertion in the file that needed the `wordpress_sites` registry, and the registry -> derivation wiring at ipc_server.rs:108-109 had no test at all. Row 3 of `list_sites_derives_front_controller_from_the_wordpress_registry` (ipc_server.rs) |
| drop | `bin/orckerd/src/ipc_server.rs` | `add_php_extension_invalid_path_rejected_before_probe` | subject `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `add_php_extension_uninstalled_version_is_not_found` | subject `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `adopt_default_if_unset_never_adopts_legacy` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `available_php_errors_on_fetch_failure` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `available_php_lists_distribution_minors_and_installed` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `available_php_lists_legacy_from_second_manifest` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `bundle_contains_ca_empty_ca_is_never_trusted` | subject `bundle_contains_ca` / `php_ca_bundle`, the bundled-PHP CA plumbing - 0 refs at HEAD (see the startup.rs rows) |
| drop | `bin/orckerd/src/ipc_server.rs` | `bundle_contains_ca_false_when_absent` | subject `bundle_contains_ca` / `php_ca_bundle`, the bundled-PHP CA plumbing - 0 refs at HEAD (see the startup.rs rows) |
| drop | `bin/orckerd/src/ipc_server.rs` | `bundle_contains_ca_matches_embedded_ca` | subject `bundle_contains_ca` / `php_ca_bundle`, the bundled-PHP CA plumbing - 0 refs at HEAD (see the startup.rs rows) |
| restore | `bin/orckerd/src/ipc_server.rs` | `check_update_falls_back_to_cache_when_offline` | **restored**, self-update not PHP: `self_update::poll_and_refresh`, `self_update::check_update`, `Response::UpdateStatus`, `UpdateSource::Cached` all survive. Only the `FailingDl` double had to come back, with the R1 rewire `orcker_php::Downloader` -> `crate::download::Downloader`. The two surviving `Cached` assertions both run against an **empty** cache, so the fallback that actually serves a cached value was uncovered |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_diagnose_flags_a_reserved_key_in_the_local_override_file` | subject a doctor finding deleted with the runtime (`NoPhpInstalled` / `ServiceOverrideInvalid`), neither in `DiagnosisCode` at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_diagnose_flags_missing_php` | subject a doctor finding deleted with the runtime (`NoPhpInstalled` / `ServiceOverrideInvalid`), neither in `DiagnosisCode` at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_doctor_fix_rebuilds_missing_php_ca_bundle` | subject `bundle_contains_ca` / `php_ca_bundle`, the bundled-PHP CA plumbing - 0 refs at HEAD (see the startup.rs rows) |
| scrub | `bin/orckerd/src/ipc_server.rs` | `dispatch_doctor_fix_with_no_pools_is_noop_but_reports_manual` | `Request::DoctorFix` is dispatched at ipc_server.rs:201 and no test reached it. Kept `performed.is_empty()`; dropped the `Severity::Fail` assertion, which relied on the deleted `NoPhpInstalled` - measured, a fresh daemon's manual list is [PortFallback, CaNotTrusted, CaNotTrustedByBrowsers, ResolverNotInstalled], all Warn. Restored as `dispatch_doctor_fix_performs_nothing_and_reports_manual` |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_dumps_status_lifecycle` | subject `Request::ListServices`/`DumpsStatus`/`ListDumps` and `Response::Services`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_list_php_no_update_when_cache_not_newer` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_list_php_reports_installed_and_default` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_list_php_surfaces_cached_update` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_list_php_surfaces_revision_autoheal` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_list_services_reports_all_engines_uninstalled` | subject `Request::ListServices`/`DumpsStatus`/`ListDumps` and `Response::Services`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_restart_all_php_no_pools_is_ok` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_set_default_php_requires_installed` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_set_default_php_sets_config_and_shim` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `dispatch_update_php_unknown_is_not_found` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `install_php_legacy_without_confirm_is_rejected` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `install_php_streamed_legacy_without_confirm_creates_no_job` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `installed_versions_empty_then_lists_fake_install` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `legacy_install_gate_blocks_only_unconfirmed_legacy` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `map_pool_state_maps_both_variants` | subject the FPM pool surface: `map_pool_state`, `pools_needing_restart`, `Response::PhpVersions`, all gone |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_checks_legacy_when_stable_is_unreachable` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_is_channel_aware_for_legacy` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| retarget | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_is_failure_tolerant` | vehicle `php_updates::poll_and_refresh` is gone, but the guarantee it pinned is documented on the surviving `self_update::poll_and_refresh` (self_update.rs:308) and untested - that fn had exactly one caller in tests, the happy path. Restored as `poll_and_refresh_leaves_the_cache_untouched_on_a_fetch_failure` |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_keeps_cache_on_unknown_schema` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_populates_cache_from_listing` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_preserves_cached_legacy_update_when_legacy_fetch_fails` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| drop | `bin/orckerd/src/ipc_server.rs` | `poll_and_refresh_tolerates_untrusted_legacy_manifest` | subject `php_updates::poll_and_refresh` and `state.php_updates` (both gone) plus the legacy/two-channel PHP listing doubles `ListingDl`/`LegacyOnlyDl`/`TwoChannelDl`/`signed_listing`. The surviving `self_update::poll_and_refresh` fetches a single latest.json and has no legacy or second-channel manifest, so these have no analogue |
| drop | `bin/orckerd/src/ipc_server.rs` | `pool_settings_are_refused_through_the_directives_path` | subject the per-version PHP settings surface: `Request::SetPhpSettings`/`SetPhpVersionSettings`/`SetPhpDirectives`/`SetPhpPoolSettings` and helpers `set_directive`/`set_pool`/`set_version_setting`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `pools_needing_restart_only_targets_active_updated_minors` | subject the FPM pool surface: `map_pool_state`, `pools_needing_restart`, `Response::PhpVersions`, all gone |
| drop | `bin/orckerd/src/ipc_server.rs` | `remove_and_list_php_extensions` | subject `Request::AddPhpExtension`/`RemovePhpExtension`/`ListPhpExtensions` and `Response::PhpExtensions`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `restart_php_not_installed_is_not_found` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_default_php_changes_fallback_for_new_sites` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_default_php_rejects_legacy_even_when_installed` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_php_directives_persists_rejects_reserved_and_removes` | subject the per-version PHP settings surface: `Request::SetPhpSettings`/`SetPhpVersionSettings`/`SetPhpDirectives`/`SetPhpPoolSettings` and helpers `set_directive`/`set_pool`/`set_version_setting`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_php_pool_settings_persists_validates_and_removes` | subject the per-version PHP settings surface: `Request::SetPhpSettings`/`SetPhpVersionSettings`/`SetPhpDirectives`/`SetPhpPoolSettings` and helpers `set_directive`/`set_pool`/`set_version_setting`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_php_settings_persists_validates_and_resets` | subject the per-version PHP settings surface: `Request::SetPhpSettings`/`SetPhpVersionSettings`/`SetPhpDirectives`/`SetPhpPoolSettings` and helpers `set_directive`/`set_pool`/`set_version_setting`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `set_php_version_settings_persists_canonicalises_and_falls_back` | subject the per-version PHP settings surface: `Request::SetPhpSettings`/`SetPhpVersionSettings`/`SetPhpDirectives`/`SetPhpPoolSettings` and helpers `set_directive`/`set_pool`/`set_version_setting`, none present |
| drop | `bin/orckerd/src/ipc_server.rs` | `site_needing_url_sync_covers_all_domain_mutations` | subject `site_needing_url_sync` (0 refs), the WordPress site-URL sync that ran after a domain change - a native-runtime feature. The `Request::*Domain` variants it took as input survive, but they are its input, not its subject |
| drop | `bin/orckerd/src/ipc_server.rs` | `site_needing_url_sync_finds_mixed_case_name` | subject `site_needing_url_sync` (0 refs), the WordPress site-URL sync that ran after a domain change - a native-runtime feature. The `Request::*Domain` variants it took as input survive, but they are its input, not its subject |
| drop | `bin/orckerd/src/ipc_server.rs` | `site_needing_url_sync_none_for_non_domain_requests` | subject `site_needing_url_sync` (0 refs), the WordPress site-URL sync that ran after a domain change - a native-runtime feature. The `Request::*Domain` variants it took as input survive, but they are its input, not its subject |
| drop | `bin/orckerd/src/ipc_server.rs` | `uninstall_php_blocked_when_default_with_others` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `uninstall_php_blocked_when_in_use_by_site` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `uninstall_php_not_installed_is_not_found` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `uninstall_php_succeeds_and_removes_dir` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| drop | `bin/orckerd/src/ipc_server.rs` | `use_overrides_parked_site_keeping_kind_mixed_case` | subject is the PHP version surface: `Request::ListPhp`/`AvailablePhp`/`InstallPhp`/`UninstallPhp`/`UpdatePhp`/`RestartPhp`/`RestartAllPhp`/`SetDefaultPhp` and helpers `installed_versions`, `fake_install`, `legacy_install_gate`, `adopt_default_if_unset`, `set_default_php` - none present at HEAD |
| retarget | `bin/orckerd/src/mutate.rs` | `set_php_on_a_proxy_name_says_it_is_a_proxy` | both assertions pin `not_found_site` (mutate.rs:67), alive and with **zero** test coverage of its proxy branch. Vehicle substituted `SetPhp` -> `SetFrontController`, whose `apply_` fn has the deleted `apply_set_php`'s exact three-branch shape. NOT `SetSecure`: it handles proxies in a third branch and never reaches the seam - measured, the first attempt failed with `got Ok(Applied{summary:"proxy reverb secure=true"})`. Restored as `a_site_only_command_on_a_proxy_name_says_it_is_a_proxy` |
| drop | `bin/orckerd/src/mutate.rs` | `set_wordpress_auto_login_records_override_keeping_parked` | subject `Request::SetWordpressAutoLogin` + `apply_set_wordpress_auto_login`, deleted (0 refs). The `Site`/`SiteOverride` `wp_auto_login` fields survive, but nothing can set them; same surviving siblings |
| drop | `bin/orckerd/src/mutate.rs` | `set_wordpress_auto_login_unknown_is_not_found` | subject `Request::SetWordpressAutoLogin` + `apply_set_wordpress_auto_login`, deleted (0 refs). The `Site`/`SiteOverride` `wp_auto_login` fields survive, but nothing can set them; same surviving siblings |
| drop | `bin/orckerd/src/mutate.rs` | `set_wordpress_auto_login_updates_linked_in_place` | subject `Request::SetWordpressAutoLogin` + `apply_set_wordpress_auto_login`, deleted (0 refs). The `Site`/`SiteOverride` `wp_auto_login` fields survive, but nothing can set them; same surviving siblings as the setphp rows |
| drop | `bin/orckerd/src/mutate.rs` | `setphp_records_override_keeping_parked` | subject `Request::SetPhp` + `apply_set_php`, deleted; the only `SetPhp` left in the tree is a stale doc reference at mutate.rs:79; the parked-override shape is pinned by `setsecure_records_override_keeping_parked` and `set_front_controller_records_override_keeping_parked` |
| drop | `bin/orckerd/src/mutate.rs` | `setphp_unknown_is_not_found` | subject `Request::SetPhp` + `apply_set_php`, deleted; the only `SetPhp` left in the tree is a stale doc reference at mutate.rs:79; pinned by `setsecure_unknown_is_not_found` and `set_front_controller_unknown_is_not_found` |
| drop | `bin/orckerd/src/mutate.rs` | `setphp_updates_linked_in_place` | subject `Request::SetPhp` + `apply_set_php`, deleted; the only `SetPhp` left in the tree is a stale doc reference at mutate.rs:79; the linked-in-place shape is pinned for surviving setters by `setsecure_updates_linked_in_place` and `set_front_controller_updates_linked_in_place` |
| retarget | `bin/orckerd/src/mutate.rs` | `upsert_merges_php_and_secure` | pins that two site-only commands on one parked site share a single override entry. `upsert` and `SetPhp` are gone; substituted the pair with the two surviving field-setters `SetSecure` + `SetFrontController`. Restored as `overrides_merge_rather_than_duplicating_for_a_parked_site` |
| drop | `bin/orckerd/src/startup.rs` | `build_php_ca_bundle_no_roots_does_not_reuse_ca_only_or_stale_bundle` | subject `reuse_existing_php_ca_bundle` (0 refs at HEAD), the reuse half of the same deleted bundle writer |
| drop | `bin/orckerd/src/startup.rs` | `build_php_ca_bundle_no_roots_reuses_existing_good_bundle_unchanged` | subject `reuse_existing_php_ca_bundle` (0 refs at HEAD), the reuse half of the same deleted bundle writer |
| drop | `bin/orckerd/src/startup.rs` | `build_php_ca_bundle_with_rootless_content_returns_none_and_does_not_write` | subject `build_php_ca_bundle` (base startup.rs, sole prod callers startup.rs:146 and ipc_server.rs:1410, both deleted). Nothing at HEAD writes `{data}/cacert.pem`; no `php_ca_bundle`/`host_roots` left in bin/orckerd |
| drop | `bin/orckerd/src/startup.rs` | `build_php_ca_bundle_with_roots_returns_path_and_writes_bundle` | subject `build_php_ca_bundle` (base startup.rs, sole prod callers startup.rs:146 and ipc_server.rs:1410, both deleted). Nothing at HEAD writes `{data}/cacert.pem`; no `php_ca_bundle`/`host_roots` left in bin/orckerd. Its composition assertions (>=2 BEGIN CERTIFICATE blocks, CA present) are pinned directly on the surviving `orcker_tls::compose_ca_bundle` at bundle.rs:43-71 |
| drop | `bin/orckerd/src/startup.rs` | `build_php_ca_bundle_without_roots_returns_none_and_does_not_write` | subject `build_php_ca_bundle` (base startup.rs, sole prod callers startup.rs:146 and ipc_server.rs:1410, both deleted). Nothing at HEAD writes `{data}/cacert.pem`; no `php_ca_bundle`/`host_roots` left in bin/orckerd |
| drop | `bin/orckerd/src/tools/mod.rs` | `move_dir_contents_moves_all_entries` | subject `move_dir_contents` (base tools/mod.rs:435), deleted with its only two callers `tools/laravel.rs:109` and `tools/wp_cli.rs:169`. `stage_and_swap` is byte-identical to base and never called it, so no surviving symbol does 'move every entry between dirs' |
| scrub | `bin/orckerd/src/tools/mod.rs` | `wp_cli_never_accepts_an_external_copy` | dead half (`Tool::WpCli.accepts_external()` + the `if t != Tool::WpCli` guard) removed; the `for t in Tool::ALL` assertion survives and pins `accepts_external`, live-gated at ipc_server.rs:734. Restored as `every_tool_accepts_an_external_copy` - the old name asserts about a deleted variant. Also reclaimed its doc comment, which SPEC-0002 orphaned onto `installed_version_ignores_blank_marker` |
| drop | `crates/orcker-doctor/src/lib.rs` | `a_clean_local_override_file_produces_no_finding` | subject `override_findings` (0 refs) and `DiagnosisCode::ServiceOverrideInvalid` (0 refs) - the doctor no longer scans service override files |
| drop | `crates/orcker-doctor/src/lib.rs` | `a_garbage_line_warns_and_names_the_line` | subject `override_findings` (0 refs) and `DiagnosisCode::ServiceOverrideInvalid` (0 refs) - the doctor no longer scans service override files |
| drop | `crates/orcker-doctor/src/lib.rs` | `a_reserved_key_warns_and_carries_its_hint` | subject `override_findings` (0 refs) and `DiagnosisCode::ServiceOverrideInvalid` (0 refs) - the doctor no longer scans service override files. Its `service_directives::reserved(OverrideDialect::MyCnf, "bind-address")` line still compiles (the pure core module survives), but that is the vehicle, not the subject, and orcker-core's own service_directives.rs carries 20 tests over it |
| drop | `crates/orcker-doctor/src/lib.rs` | `a_service_without_a_dialect_is_skipped` | subject `override_findings` (0 refs) and `DiagnosisCode::ServiceOverrideInvalid` (0 refs) - the doctor no longer scans service override files |
| drop | `crates/orcker-doctor/src/lib.rs` | `an_override_finding_suppresses_the_all_good_line` | subject `override_findings` (0 refs) and `DiagnosisCode::ServiceOverrideInvalid` (0 refs) - the doctor no longer scans service override files; the AllGood-suppression rule it also touched is pinned by the surviving `problems_suppress_all_good` |
| drop | `crates/orcker-doctor/src/lib.rs` | `default_not_installed_when_other_versions_present` | subject `DiagnosisCode::DefaultPhpNotInstalled`, absent from the enum at status.rs:448-485 |
| drop | `crates/orcker-doctor/src/lib.rs` | `failed_pool_is_fail_and_auto_fixable` | subject `DiagnosisCode::FpmPoolFailed` + `FixAction::RestartFpm`, both gone (`plan_auto_fixes` is now a stub returning `Vec::new()`, lib.rs:203). Its tail assertion `!is_auto_fixable(CaNotTrusted)` does compile, but four surviving tests already assert that same shape (lib.rs:499, 522, 545, 838) |
| drop | `crates/orcker-doctor/src/lib.rs` | `no_php_suppresses_default_not_installed` | subject `DiagnosisCode::NoPhpInstalled`, absent from the enum |
| drop | `crates/orcker-doctor/src/lib.rs` | `php_ca_none_or_true_emits_no_finding` | subject `DiagnosisCode::PhpCaNotTrusted` + `FixAction::RebuildPhpCaBundle`, both gone; `php_trusts_ca` survives only as a GUI TypeScript field |
| drop | `crates/orcker-doctor/src/lib.rs` | `php_ca_untrusted_warns_and_plans_rebuild` | subject `DiagnosisCode::PhpCaNotTrusted` + `FixAction::RebuildPhpCaBundle`, both gone |
| drop | `crates/orcker-doctor/src/lib.rs` | `update_available_is_informational_and_still_all_good` | subject `DiagnosisCode::PhpUpdateAvailable`, absent from the enum. Its general rule (an informational finding does not suppress AllGood) is already covered twice - proven, not assumed: widening the AllGood severity gate failed `resolver_backup_surfaces_as_ok_finding_with_no_remedy` and `symlink_protection_off_surfaces_as_ok_finding_with_no_remedy` together with a trial retarget, so the retarget was withdrawn as a duplicate |
| drop | `crates/orcker-mcp/tests/tools.rs` | `create_site_defaults_match_the_gui_wizard` | subject `Request::CreateSite` + `orcker_ipc::Framework::Laravel{options}` (`StarterKit`, `Testing`, `Database`, `JsRuntime`, `AuthProvider`), all absent from request.rs; the `create_site` tool is not in the catalog `catalog_names_are_pinned` pins |
| drop | `crates/orcker-mcp/tests/tools.rs` | `create_site_options_are_mapped` | subject `Request::CreateSite` + `orcker_ipc::Framework::Laravel{options}` (`StarterKit`, `Testing`, `Database`, `JsRuntime`, `AuthProvider`), all absent from request.rs; the `create_site` tool is not in the catalog `catalog_names_are_pinned` pins |
| drop | `crates/orcker-mcp/tests/tools.rs` | `optional_arguments_reach_the_request` | 7 of its 8 assertions target `create_site`'s option set and `list_dumps`/`since_id`, none of which is in the pinned catalog. Its 8th (`job_status` honours `cursor`) is pinned strictly more precisely by the surviving `paging_tools_default_their_cursors_to_zero`, which asserts the resulting `cursor: 12` rather than merely != default |
| drop | `crates/orcker-mcp/tests/tools.rs` | `php_tools_map_to_their_requests` | subject `Request::InstallPhpStreamed`, `Request::SetDefaultPhp`, `Request::SetPhpSettings` - none in request.rs; the tools `install_php`, `set_default_php`, `set_php_setting` are not in the pinned catalog |
| scrub | `crates/orcker-mcp/tests/tools.rs` | `read_tools_map_to_their_requests` | restored verbatim first: exactly 5 of 12 assertions failed to compile (`ListPhp`, `AvailablePhp`, `ListServices`, `DumpsStatus`, `ListDatabases`), the other 7 compiled. Kept those 7 - the name -> variant mapping no surviving test pins (`every_tool_builds_a_request` only proves a request is built) |
