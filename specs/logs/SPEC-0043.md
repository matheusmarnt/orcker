# Cycle log — SPEC-0043

Spec: `specs/SPEC-0043-orcker-stack-instruction-file.md`
Branch: `feat/SPEC-0043-orcker-stack-instruction-file`
Surface: `.github/`
Approval commit (DT9): `eea623a` (`docs(specs): approve SPEC-0043 orcker-stack
instruction file`), ahead of the implementation commit that follows this log.

## S1 — spec amendment

The committed spec carried only a `## Context`, no `## Requirements` or
`## Acceptance checklist` (required by `docs/SDD.md` section 4). Asked the
human how to proceed; chose "amend spec, then implement". Added R1-R4 and
AC1-AC5 in `da4aab3`, derived directly from the existing Context — no new
product decisions.

## S2 — plan

Explored `crates/orcker-stack` and `crates/orcker-engine` (two parallel
Explore agents) plus the existing `.github/instructions/orcker-core.instructions.md`,
`orcker-ipc.instructions.md`, `orcker-doctor.instructions.md`, and
`orcker-proxy.instructions.md` as style references, and verified the
`StatusReport` literal sites with:

```
git grep -nE 'StatusReport \{' -- '*.rs'
```

Plan: write two new instruction files mirroring the existing per-crate shape
(Layer, Owns, Must not, Conventions, Tests/invariants, Review checklist), and
amend `orcker-ipc.instructions.md` with a `StatusReport` trap section under
*Contract rules*.

## S3 — RED evidence

Ran each AC's evidence command against the tree before writing any file:

```
$ test -f .github/instructions/orcker-stack.instructions.md; echo "exit: $?"
exit: 1
$ test -f .github/instructions/orcker-engine.instructions.md; echo "exit: $?"
exit: 1
$ grep -n 'nine' .github/instructions/orcker-ipc.instructions.md; echo "grep exit: $?"
grep exit: 1
$ grep -n 'non_exhaustive\|Default\|SPEC-0020' .github/instructions/orcker-ipc.instructions.md; echo "grep exit: $?"
grep exit: 1
```

All four fail, as expected for a doc-only spec with nothing written yet.

## S4 — implement (GREEN)

Wrote `.github/instructions/orcker-stack.instructions.md` and
`.github/instructions/orcker-engine.instructions.md`, and amended
`.github/instructions/orcker-ipc.instructions.md` with the `StatusReport`
trap paragraph (nine literal sites, five crates, one production site, two
non-fixes, SPEC-0020 warning).

Correction during self-check: the first draft listed
`crates/orcker-doctor/src/lib.rs` as having two `StatusReport { ... }`
literal sites. Re-reading the file showed `rootless_fallback()` calls
`healthy()` and mutates the clone (`PortStatus` field writes) rather than
building a fresh `StatusReport` literal — only `healthy()` is a real
construction site. Fixed before commit; the nine-site count is unaffected
(the second `wire_stability.rs` site absorbs no change — it was already
counted separately).

GREEN evidence:

```
$ test -f .github/instructions/orcker-stack.instructions.md; echo "exit: $?"
exit: 0
$ grep -c '0\.0\.0\.0\|privileged\|docker\.sock' .github/instructions/orcker-stack.instructions.md
3
$ grep -n 'bollard\|MIN_ENGINE_VERSION\|MIN_COMPOSE_VERSION\|minimum_version_policy' .github/instructions/orcker-engine.instructions.md
(all four terms present, multiple lines)
$ git grep -nE 'StatusReport \{' -- '*.rs'
bin/orcker/src/map.rs:2446:    fn sample_report() -> orcker_ipc::StatusReport {
bin/orcker/src/map.rs:2447:        orcker_ipc::StatusReport {
bin/orcker/src/mcp_cmd.rs:360:    fn report_with(mcp_enabled: bool) -> StatusReport {
bin/orcker/src/mcp_cmd.rs:361:        StatusReport {
bin/orckerd/src/ipc_server.rs:473:async fn build_status_report(state: &DaemonState) -> orcker_ipc::StatusReport {
bin/orckerd/src/ipc_server.rs:591:    orcker_ipc::StatusReport {
crates/orcker-doctor/src/lib.rs:418:    fn healthy() -> StatusReport {
crates/orcker-doctor/src/lib.rs:419:        StatusReport {
crates/orcker-doctor/src/lib.rs:639:    fn rootless_fallback() -> StatusReport {
crates/orcker-ipc/src/response.rs:630:            report: Box::new(crate::status::StatusReport {
crates/orcker-ipc/src/status.rs:25:pub struct StatusReport {
crates/orcker-ipc/tests/roundtrip.rs:157:        report: Box::new(StatusReport {
crates/orcker-ipc/tests/wire_stability.rs:560:        report: Box::new(StatusReport {
crates/orcker-ipc/tests/wire_stability.rs:695:fn sample_status_report() -> StatusReport {
crates/orcker-ipc/tests/wire_stability.rs:696:    StatusReport {
crates/orcker-mcp/tests/render.rs:206:fn sample_report() -> orcker_ipc::StatusReport {
crates/orcker-mcp/tests/render.rs:207:    orcker_ipc::StatusReport {

Raw output is 17 lines, not 9: the pattern also matches 7 function
signatures ending `-> StatusReport {` (map.rs:2446, mcp_cmd.rs:360,
ipc_server.rs:473, doctor/lib.rs:418, doctor/lib.rs:639,
wire_stability.rs:695, render.rs:206) and the struct definition itself
(status.rs:25) — none of those eight construct a `StatusReport` value. The
nine actual construction sites, matching the instruction file, are:
map.rs:2447, mcp_cmd.rs:361, ipc_server.rs:591, doctor/lib.rs:419,
response.rs:630, roundtrip.rs:157, wire_stability.rs:560,
wire_stability.rs:696, render.rs:207.
$ grep -n 'non_exhaustive\|Default\|SPEC-0020' .github/instructions/orcker-ipc.instructions.md
(all three present)
```

## S5 — gate

```
scripts/gate.sh specs/SPEC-0043-orcker-stack-instruction-file.md
```

Result: `[gate] OK`. Step 5 (clippy-allow baseline) not touched — no `.rs`
file added, moved, deleted, or renamed by this diff.

## S6 — self-review

- AC1: `orcker-stack.instructions.md` exists, states pure/no-I/O/no-async/no-
  `orcker-*`, and names all three container bans (`0.0.0.0`, `privileged`,
  `docker.sock`) — 3 matches, satisfies "3 or more".
- AC2: `orcker-engine.instructions.md` names `bollard`-only-here-no-TLS and
  both version-floor constants plus `minimum_version_policy`.
- AC3: the nine `StatusReport { ... }` sites are enumerated in
  `orcker-ipc.instructions.md` and match `git grep` exactly; the one
  production site (`ipc_server.rs`) is called out by name.
- AC4: both non-fixes (`#[non_exhaustive]`, `derive(Default)`) and SPEC-0020
  are named.
- AC5: gate passed above.
- Surface: diff touches only `.github/` (plus `specs/`), matching the spec's
  declared surface.

## S7 — round 2 (supervisor REWORK)

Verdict: REWORK, two judgment findings (JG8), no deterministic-check failure.

1. Amending an already-`approved` spec (`da4aab3`) mid-cycle needed a
   `specs/DECISIONS.md` entry, not only this log — a cycle log closes at S8;
   `DECISIONS.md` is the cross-cycle record and this exact situation (an
   approved spec with no Requirements/Acceptance) can recur. Added the
   2026-09-05 SPEC-0043 entry, following the 2026-08-22 SPEC-0001 precedent
   shape.
2. The S4 GREEN evidence for AC3 pasted a filtered 9-line list under the raw
   `git grep` command without saying so; the real output is 17 lines (adds 7
   function-signature matches and the struct definition). Replaced with the
   full unfiltered output plus an explicit note distinguishing the 9
   construction sites from the 8 non-construction matches.

No code, instruction-file content, or AC evidence changed — both findings
were about the log/record, not the implementation. Re-ran the gate to
confirm nothing regressed:

```
scripts/gate.sh specs/SPEC-0043-orcker-stack-instruction-file.md
```

Result: `[gate] OK`.
