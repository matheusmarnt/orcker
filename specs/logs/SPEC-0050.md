# Cycle log — SPEC-0050

Spec: `specs/SPEC-0050-ipc-variant-literal-precheck.md`
Branch: `feat/SPEC-0050-ipc-variant-literal-precheck`
Surface: `.github/`
Approval commit (DT9): `ea970ad` (`docs(specs): approve SPEC-0050 IPC variant literal precheck`), ahead of this branch's history.

## S1 — spec amendment

Added the missing *FR acceptance* line to the acceptance checklist (its
absence REWORKed SPEC-0055). FR-002 has AC1/AC2 in `docs/PRD.md`, both already
closed by SPEC-0002; this cycle closes no new FR acceptance, and AC3's gate
run merely re-exercises AC1, it does not close it independently.

## S2 — plan

Single-file docs change to `.github/instructions/orcker-ipc.instructions.md`:
a new "Precheck before adding a field to an existing variant" subsection under
*Contract rules*, ahead of the existing `StatusReport` trap section (which
becomes the worked example). Content: the precheck command (R1), the escape
hatch with both precedents verified present in the tree — `orcker status`
(`Status`/`EngineStatus`) and `orcker sites` (`ListSites`/`ListProjects`) (R2)
— and both named instances, SPEC-0004/`StatusReport` and
SPEC-0006/`Response::Sites` (R3).

## S3 — RED evidence

```
$ grep -n 'StatusReport\|Response::Sites' .github/instructions/orcker-ipc.instructions.md; echo "exit: $?"
37:## The `StatusReport` trap
39:`StatusReport` is **not** `#[non_exhaustive]` and is built with a full struct
46:every one of them at once. A spec that intends to extend `StatusReport` must
48:`git grep -nE 'StatusReport \{' -- '*.rs'` before writing the design.
55:takes a `&StatusReport`.
exit: 0
```

`Response::Sites` does not appear anywhere in the file pre-edit — AC2 fails
(only one of the two required names is present).

```
$ grep -n 'Request|Response)::<Variant>' .github/instructions/orcker-ipc.instructions.md; echo "exit: $?"
exit: 1
```

The precheck command itself is absent pre-edit — AC1 fails.

## S4 — implement (GREEN)

Edit: `.github/instructions/orcker-ipc.instructions.md`, new subsection added
(see S2). Diff is additive only; the existing `StatusReport` trap section is
unchanged text, just relocated to follow the new subsection.

```
$ grep -n 'StatusReport\|Response::Sites' .github/instructions/orcker-ipc.instructions.md; echo "exit: $?"
52:Two cycles paid for skipping this: SPEC-0004 wanted a field on `StatusReport`
54:`Response::Sites`, blocked by a literal in `crates/orcker-mcp/tests/render.rs`
58:## The `StatusReport` trap
60:`StatusReport` is **not** `#[non_exhaustive]` and is built with a full struct
67:every one of them at once. A spec that intends to extend `StatusReport` must
69:`git grep -nE 'StatusReport \{' -- '*.rs'` before writing the design.
76:takes a `&StatusReport`.
exit: 0
```

AC2 GREEN: both names present.

```
$ git grep -nE '(Request|Response)::Sites \{' -- '*.rs' | wc -l
41
```

AC1 GREEN: the precheck command (substituting `Sites` for `<Variant>`) runs
clean against the current tree — 41 hits across 11 files (`apps/orcker-gui`,
`bin/orcker`, `bin/orckerd`, `crates/orcker-ipc`, `crates/orcker-mcp`), none
inside this spec's own `.github/` surface. This is also a live demonstration
of the rule the note teaches: a field added to `Response::Sites` today would
need every one of those files declared, exactly the trap SPEC-0006 hit.

## S5 — gate

```
$ scripts/gate.sh specs/SPEC-0050-ipc-variant-literal-precheck.md
[gate 1/6] rustfmt
[gate 2/6] clippy (deny warnings)
[gate 3/6] tests
... 2017 passed / 0 failed (summed across all workspace test binaries), 0 FAILED
[gate 4/6] gui (only when touched)
[gate 5/6] no new clippy escape hatches
[gate 6/6] surface check
[surface] allowed prefixes: specs/ docs/ scripts/clippy-allow-baseline.txt .github/
[gate] OK
```

Step 5 (clippy-allow baseline) not touched — no `.rs` file added, moved,
deleted or renamed by this diff, so the ratchet list is byte-identical and was
left alone.

## S6 — self-review

- Diff is one file, `.github/instructions/orcker-ipc.instructions.md`, plus
  this log and the spec's own status/FR-acceptance lines — all inside
  `surface: [.github/]` (plus the standing `specs/`/`docs/` allowance).
- R1/R2/R3 all present and named; both escape-hatch precedents
  (`bin/orcker/src/lib.rs:234-267`, `bin/orcker/src/lib.rs:304-318`,
  `bin/orcker/src/map.rs:989-1002`) verified against the tree before writing,
  not asserted from memory.
- `scripts/gate.sh` untouched (DT7); no existing test weakened.
- ROADMAP.md row 50 intentionally left at `draft` — it flips to `accepted`
  only in the S8 acceptance commit, matching SPEC-0043's precedent (`f01937b`).

## S7 — supervisor (round 1)

```yaml
spec: SPEC-0050
verdict: APPROVE
deterministic:
  DT1_gate: pass
  DT2_surface: pass
  DT3_ac_mapping: pass
  DT4_red_evidence: pass
  DT5_ipc_stability: pass
  DT6_deps: pass
  DT7_gate_integrity: pass
  DT8_public_docs: pass
  DT9_approval_commit: pass
  DT10_fr_ac_coverage: pass
acceptance:
  AC1: { status: pass, evidence: "precheck present at orcker-ipc.instructions.md:40; `git grep -nE '(Request|Response)::Sites \\{' -- '*.rs'` = 41 hits / 11 files, none under .github/; RED exit 1 pre-edit reproduced by supervisor" }
  AC2: { status: pass, evidence: "grep -n 'StatusReport\\|Response::Sites' hits lines 52,54,58,60,67,69,76; pre-edit `Response::Sites` count 0 (RED reproduced); escape hatch verified at bin/orcker/src/lib.rs:249,267,307,318 and map.rs:998" }
  AC3: { status: pass, evidence: "scripts/gate.sh specs/SPEC-0050-ipc-variant-literal-precheck.md -> [gate] OK, exit 0, 2017 passed / 0 failed" }
judgment_findings: []
regression_notes: "No code touched: diff is one markdown file plus specs/, so zero runtime regression surface. Cycle log S4 stated '12 files' where the verified count is 11 (the recorded `wc -l` output of 41 was correct, AC1's conclusion unaffected) - corrected to 11 at S8. Standing risk, not introduced here: the precheck note is prose with no gate check behind it, so a future variant rename cannot invalidate it mechanically; the same already applies to the adjacent StatusReport trap section."
escalate_reason: null
```
