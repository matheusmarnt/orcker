# Traceability matrix — FR <-> spec <-> tests <-> commit

Updated at S8 of every accepted cycle (see docs/SDD.md sections 6 and 11).
One row per accepted spec. `Attempts` counts REWORK rounds (0 = first pass).
The `Commit` column names the commit subject: the hash cannot be recorded inside
the commit that this row is part of.

| Spec | Covers | Attempts | Escalates | Gate time | Pure-crate coverage | Cycle duration | Final verdict | Key tests | Commit |
|------|--------|----------|-----------|-----------|---------------------|----------------|---------------|-----------|--------|
| SPEC-0001-fork-bootstrap | FR-001 | 0 | — | — | — | 1 session (2026-08-22) | APPROVE | inherited suite, 2602 passed / 0 failed; repaired `orcker_platform::pure::dns_probe::tests::query_encodes_probe_name_and_a_question` and `orckerd::self_update::tests::current_version_parses` | `chore(workspace)!: rebrand the Yerd fork as Orcker` on `feat/SPEC-0001-fork-bootstrap` |
| SPEC-0032-pin-gate-sort-collation | FR-001 | 0 | — | — | — | 1 session (2026-08-22) | APPROVE | no Rust changed; the gate itself is the test: `scripts/gate.sh` run twice on one tree, `LC_ALL=pt_BR.UTF-8` and `LC_ALL=C`, exit 0 both, 89 `test result: ok` lines each. Negative reproduction: a bare `pt_BR` sort still fails the step-5 diff | `fix(scripts): pin the gate clippy-allow sort to the C collation` on `feat/SPEC-0001-fork-bootstrap` |
| SPEC-0002-remove-native-runtime | FR-002 | 6 | — | — | — | 1 session (2026-08-22) | APPROVE | `cargo test --workspace` 1940 passed / 0 failed and GUI 397 passed / build ok, both via the gate. New guards, each with RED evidence in the cycle log: `apps/orcker-gui/tests/commandContract.test.ts` (every invoked Tauri command is registered; 49 known-dangling enumerated as a bidirectional ratchet in `src/ipc/dangling-commands.json`) and `apps/orcker-gui/tests/routeTargets.test.ts` (every navigation target resolves in `router.ts`, scanning both the Vue/TS forms and the Rust tray). `orcker-ipc` wire reset verified deletion-only bar one declared retarget: 8 added / 1403 removed | `refactor(workspace)!: remove the native runtime` on `feat/SPEC-0002-remove-native-runtime` |
| SPEC-0033-remove-stale-php-instruction-file | FR-002 | 1 | — | — | — | 1 session (2026-08-22) | APPROVE | docs-only, so the acceptance test is the grep run before and after: AC1 `orcker-php` / `orcker-services` / `orcker-supervise` over `.github/` RED at 5 matches / exit 0, GREEN exit 1; a second R2 criterion over native-runtime wording RED at 3 / exit 0, GREEN exit 1. Gate `[gate] OK`, 76 `test result: ok`, 0 failed. Supervisor re-verified R2 independently with a whole-file whitespace-normalised sweep, since the line-based grep cannot see wrapped text | `docs(github): retire the instruction surface for the native runtime` on `feat/SPEC-0033-remove-stale-php-instructions` |
| SPEC-0036-remove-dead-gui-surfaces | FR-002 | 3 | — | — | — | 1 session (2026-08-23) | APPROVE | Driving guard is SPEC-0002's `apps/orcker-gui/tests/commandContract.test.ts`, unmodified across all four rounds: emptying `src/ipc/dangling-commands.json` took it RED at 49 dangling invocations (reproduced by the supervisor in a throwaway `HEAD` worktree), GREEN once every call site was gone. Gate `[gate] OK`; GUI 34 files / 324 tests (341 -> 324 is exactly `phpSettings.test.ts`'s 17, ledgered in `specs/logs/SPEC-0036-deleted-tests.md`); `cargo test --workspace` 0 failed. New criterion added mid-cycle by human authorisation after an ESCALATE: R1 had no executable completeness check, because the scan the spec named holds SPEC-0002's symbols and cannot detect an orphan SPEC-0036 creates. `apps/orcker-gui/tests/dead-export-delta.mjs` (R0b/AC6) reports `dead(tree) \\ dead(HEAD)`; it found 5 orphans where three review rounds found 2, then a second-order one (`ComboboxOption`) that did not exist until the first deletion landed, and was iterated to a fixed point: 15 -> 9, all 9 `Extract<>` aliases over `Response` members deferred to SPEC-0034/0035. The absolute-ratchet alternative was built, run and rejected on its own output (~84-entry allowlist, same-file helpers misreported, and it demanded deleting the `JobState` that R3 mandates). Spawned SPEC-0039 R3, SPEC-0038 R5/AC4 and SPEC-0040 | `refactor(gui)!: remove the surfaces SPEC-0002 left without a backend` on `feat/SPEC-0036-remove-dead-gui-surfaces` |
| SPEC-0037-restore-coverage-deleted-by-spec-0002 | FR-002 | 0 | — | — | — | 1 session (2026-08-30) | APPROVE | Test-only diff: 0 production lines changed. All 136 tests SPEC-0002 deleted from a surviving file were dispositioned one at a time - 123 drop / 6 retarget / 5 scrub / 2 restore - and 11 came back: `renders_human_responses_and_exit_codes` (restored **verbatim**, the `orcker status` renderer SPEC-0002's own AC4 cited), `check_update_falls_back_to_cache_when_offline` (the only test of a *populated* cache surviving an offline fetch), `maps_each_command_to_its_request` (the core CLI mapping had **zero** `to_request` coverage), `rejects_a_bad_site_name_before_connect`, `read_tools_map_to_their_requests`, `list_sites_derives_front_controller_from_the_wordpress_registry`, `a_site_only_command_on_a_proxy_name_says_it_is_a_proxy` (`not_found_site`'s proxy branch, previously untested), `overrides_merge_rather_than_duplicating_for_a_parked_site`, `poll_and_refresh_leaves_the_cache_untouched_on_a_fetch_failure`, `dispatch_doctor_fix_performs_nothing_and_reports_manual`, `every_tool_accepts_an_external_copy`. `cargo test --workspace` 1939 -> 1950 passed / 0 failed; gate `[gate] OK`. Scrub boundaries were **measured, not judged**: each was restored verbatim first and the compiler named the dead assertions (53 errors in `map.rs`, 5 in `orcker-mcp/tests/tools.rs`), which also caught a pruned `use std::path::PathBuf` that R1 required restoring rather than dropping. Every added test carries inversion RED in the cycle log. One proposed retarget was **withdrawn** when its inversion failed two surviving `orcker-doctor` tests as well, proving the rule already covered - that crate ends with zero diff. Supervisor verified the 123 drops decisively rather than by sampling: all 121 non-Tauri dropped tests restored into a scratch worktree and compiled, 120 fail to compile and the 1 that compiles fails at runtime on `unknown tool create_site`. Spawned SPEC-0041 (a deleted `Command::Exec`'s doc block now documents `Command::Mcp`, so `orcker help mcp` prints it) | `test(workspace): dispose of every test SPEC-0002 deleted from a surviving file` on `feat/SPEC-0037-restore-deleted-coverage` |
| SPEC-0003-stack-compose-renderer | FR-022 (partial) | 0 | — | — | — | 1 session (2026-08-30) | APPROVE | First new crate of the fork, 4 acceptance tests over a 100% pure library: `orcker_stack::compose::reference_postgres_snapshot` (byte-equality against the committed 56-line `tests/fixtures/compose_reference_postgres.yml`), `::deterministic_output`, `::loopback_only_ports` (also asserts no `privileged`, no `0.0.0.0`, no `docker.sock`) and `orcker_stack::validate::rejects_invalid_inputs` (8 site-name + 6 PHP + 3 port negative cases, every reason pinned). RED was taken against stub bodies, not compile errors, and the supervisor reproduced it in a throwaway worktree (`render_compose -> String::new()` gives 1 passed / 2 failed, matching the log). AC2 is disclosed in the log as weak by nature: no deterministic stub can drive it RED, so R4 rests on construction - `render_reference` is one `format!` over a const template with no map iteration. AC5 run twice, by the cycle and independently by the supervisor: `docker compose config` exits 0 with **empty stderr**, resolving `host_ip: 127.0.0.1` on both published ports. The `$$` escape in the `pg_isready` healthcheck was verified empirically rather than assumed, with a two-service probe proving `$$` blocks host interpolation while `$` does not. `cargo test --workspace` 0 failed; gate `[gate] OK`. Spawned SPEC-0042 (`referenciadockerlaravel.md` is cited by `docs/PRD.md:14` but absent from the repo) and SPEC-0043 (the new crate has no `.github/instructions` file) | `feat(stack): render the reference Postgres compose from a typed model` on `feat/SPEC-0003-stack-compose-renderer` |
| SPEC-0042-restore-missing-reference-doc | FR-022 | 0 | — | — | — | 1 session (2026-08-30/31) | APPROVE (round 3, once the human cleared the sole open item) | Docs-only, no Rust: the six ACs are commands, each RED-reproduced by the supervisor read-only from `HEAD` blobs (AC1/AC5 absent from `HEAD`; AC3 1 match each in SPEC-0003/0005; AC4 0 matches of the real path). Index blob `997588a5`, 405 lines, identical to the worktree; independent secrets/PII scan clean. Three supervisor rounds, none a REWORK, so `Attempts` stays 0 and understates the cycle: round 1 ESCALATE on authority (the spec was `draft` at `HEAD` while the working tree showed `approved` from an uncommitted edit of unknown authorship, and the cycle had authored its own R#/AC#); round 2 ESCALATE plus a DT3 failure the round-1 repair itself caused - the `specs/DECISIONS.md` entry the supervisor asked for became an unlisted survivor of R4's file enumeration, which had already drifted at S3 and S4; round 3 clean after R4/AC3 were restated as a rule with no file list. The human-only `draft -> approved` transition was absent from the committed record and is not verifiable by a subagent; it was supplied as its own commit (`docs(specs): approve SPEC-0042 reference-document restore`) before this one. Gate `[gate] OK`, 1954 passed / 0 failed | `docs(reference): commit the docker/Laravel reference document and repoint its citations` on `feat/SPEC-0042-restore-missing-reference-doc` |
| SPEC-0045-verifiable-human-approval | FR-001 | 1 | — | — | — | 1 session (2026-08-31) | APPROVE (round 2) | Process-only, no Rust: the seven ACs are commands, each RED before the edits and GREEN after, quoted in the cycle log and reproduced independently by the supervisor via revert sampling against `HEAD` (`draft -> approved`, `DT9`, `apenas rodadas de` all 0 matches at `HEAD`; `git show` absent from `HEAD:.claude/commands/spec-next.md`). AC3 is a genuine behavioural negative reproduction rather than a grep asserting its own edit: in a throwaway repo, one spec file committed `draft` and flipped to `approved` uncommitted yields `draft` under the new `git show HEAD:` rule and `approved` under the working-tree read it retires - run by the cycle and again by the supervisor in its own scratch repo. This spec is the first subject of its own R1: the human approval is commit `a115cef`, ahead of this one, `--numstat` exactly `1 1` on the spec and `1 1` on `ROADMAP.md`. Round 1 was a REWORK for carrying the "an embedded section-9 copy must not contradict the section it copies" argument to SDD 9.2 but not to 9.3, which would have regenerated a supervisor that never checks DT9; the repair also closed the ROADMAP header, the third copy of the retired rule. Gate `[gate] OK`, no `.rs`/`Cargo.*`/`package.json` touched | `docs(process): make the human-only approval verifiable in the committed record` on `feat/SPEC-0045-verifiable-human-approval` |
| SPEC-0005-proxy-container-spike | FR-003 (partial) | 1 | — | — | — | 1 session (2026-08-31) | APPROVE (round 2) | Evidence-driven spike, no unit target: R2's conditional code delta never triggered because `bin/orcker/src/cli.rs:274` `ProxyAction::Add` already expresses a whole-host loopback upstream, so the declared code surface stayed untouched and the diff is docs + specs only. RED is `docs/spike/evidence/red-before-spike.txt` captured at `bbfb7d2` (host unresolved, curl exit 6/7, no listeners on 80/443/18080/5173), flipped by the same commands in `green-stack-up.txt` and `elevated-443.txt`. AC2 is `curl -sv https://spike.test/` with **no** `--cacert`: `200`, `subject: CN=spike.test`, `issuer: CN=Orcker Local CA`, `SSL certificate verify ok`, and `http://` answering `301`. AC4 is `101 Switching Protocols` through the proxy with `sec-websocket-accept` and the `vite-hmr` subprotocol intact. Eight findings recorded (F1-F8), three of them bugs the SPEC-0007 preset must not inherit from `docs/referencia-docker-laravel.md` (`libonig-dev`, the `postgres:18` mount path, Vite `allowedHosts`), one a real product gap queued as SPEC-0046 (`elevate` ignores the environment under `sudo` and cannot reach an XDG-isolated dev instance). Round 1 was a REWORK: the cycle retracted its own cold-opcache latency measurement in the runbook and cycle log but left the superseded 'no measurable overhead' claim in `DECISIONS.md` and the SPEC-0006 Context note - the two records the next cycle actually reads. Corrected to ~25.7-31.3 ms direct vs 113-125 ms proxied on a debug build, gap attributed to the per-request TLS handshake and release-build cost left explicitly unmeasured. Gate `[gate] OK`, 80 test targets green, no `.rs`/`Cargo.*`/`package.json` touched | `docs(spike): serve a containerized Laravel app at https://spike.test through the inherited proxy` on `feat/SPEC-0005-proxy-container-spike` |
| SPEC-0047-traceability-record-repair | FR-001 | 2 | 0 | 13s | — | 1 session (2026-08-31) | APPROVE (round 3) | Record-only, no Rust: AC1 is five per-column regexes over the ten rows, and it is discriminating rather than decorative — the supervisor ran it against a reconstructed pre-fix table and got 10 violations, against the repaired one 0. That test exists because round 1's evidence was a field count, which a one-column shift preserves: the insert was written as `cells[:5] + three dashes + cells[5:]` when index 4 of a split row is `Cycle duration`, not `Attempts`, so every row moved one column left, `Escalates` inherited `1 session (<date>)` and the one value the record held was overwritten with a dash. Round 2 was a REWORK for a shared record written in the wrong language: the DECISIONS entry was Portuguese in a file whose 395 prior lines are English. RED at `50d7d85` also exposed a defect the spec had not named — one row at 11 fields against a 9-field header, caused by two regex alternations inside backticks whose raw pipes are cell separators. The all-dash backfill is a refusal, not an omission: a word-count grep returns 15 for SPEC-0045 (the spec about escalation) and `rounds - attempts` yields 2 for SPEC-0042 whose prose says three, so both recoveries were rejected and cited in the log. Gate `[gate] OK`, diff confined to `specs/`, `docs/PRD.md` untouched | `docs(specs): make the traceability record answer SDD section 11` on `feat/SPEC-0047-traceability-record-repair` |

| SPEC-0004-engine-docker-detection | FR-010 | 1 | 0 | 15s | — | 1 session (2026-08-31) | APPROVE (round 2) | First I/O-edge crate of the fork. AC1-AC3 are three table-driven pure tests (`orcker_engine::pure::tests::socket_resolution_matrix` 9 rows, `::compose_version_parsing` 9 rows, `::minimum_version_policy`), AC4 five additive `orcker_ipc` byte pins (`request_engine_status_byte_shape`, `response_engine_status_{healthy,degraded}_byte_shape`, `engine_problem_code_each_tag_byte_shape`, `engine_unit_variants_byte_shape`) with the 120 pre-existing pins untouched, AC5 five `orcker` renderer tests plus live `orcker status` output against real Docker (engine 29.7.2 / compose 5.5.0) and against an unreachable socket, both exit 0. Side effects sit behind `EngineApi` / `ComposeCli` with five fake-driven integration tests in `crates/orcker-engine/tests/assemble.rs`. RED was taken against stub bodies returning `None`/`vec![]`/`String::new()`, not compile errors; AC4's RED is disclosed as a compile error and reproduced by stashing only `crates/orcker-ipc/src`. Two forced deviations, both in `DECISIONS.md`: R2's model ships as `orcker_ipc::DockerStatus` because `StatusReport` is not `#[non_exhaustive]` and `crates/orcker-mcp/tests/render.rs` builds it with a full struct literal outside the surface, and `bollard` enables **no** TLS feature (stricter than the spec's rustls-only; 5 new lock entries, zero TLS crates). `Response::EngineStatus` boxes its payload after the inline variant tripped `result_large_err` in three `bin/orckerd` sites - wire bytes unchanged, verified by the pins passing before and after. Round 1 was a REWORK on two judgment findings: `hint_for` hardcoded `24.0`/`2.20` beside the `MIN_*_VERSION` constants R5 makes the sole floor (fixed by interpolating them, with an assertion that keeps it that way), and `run_status`'s `_ => None` made the CLI print "the daemon predates the docker section" for what is usually a transport failure (fixed by the pure `map::docker_section`, four outcomes covered). Both repairs carry their own RED. Gate `[gate] OK`. Coverage column is `—` and the reason is ledgered: no tool installed, queued as SPEC-0048 | `feat(engine): detect the Docker environment and report it in `orcker status`` on `feat/SPEC-0004-engine-docker-detection` |
| SPEC-0006-link-loopback-port | FR-021, FR-013 (partial) | 2 | 0 | 43s | — | 1 session (2026-08-31) | APPROVE (round 3) | Closes the Phase-0 golden thread. AC1 is two pure table suites (`orcker_core::ports::tests::allocation_matrix` over free/persisted/busy/mixed with every case asserted twice for determinism, plus `exhausted_range_is_a_typed_error`) and the persistence pair `orcker_config::ports::tests::allocation_roundtrip` / `unlinking_frees_the_port`; AC2 is `orcker_config::orcker_yml::tests::v1_roundtrip_and_forward_compat` plus six negative cases in `invalid_values_are_rejected`, over a hand-written strict reader because the spec withheld authorization for a YAML crate; AC3 is `orckerd::link::tests::idempotent_relink` against a fake `PortProbe` and an in-memory config. R1 shipped as a deviation: `orcker-core` gained `ContainerProject` projecting onto the existing `ProxySite` rather than a third `Route` arm, because a new site kind needed `crates/orcker-proxy/`, outside the surface, and SPEC-0005 had already proved the proxy path end to end with zero code. R7 likewise: `Response::Sites` was first given a `projects` field, which broke `crates/orcker-mcp/tests/render.rs` (a full struct literal outside the surface, the same trap SPEC-0004 hit with `StatusReport`), so projects ship as `Request::ListProjects` / `Response::Projects` and `orcker sites` merges two replies the way `orcker status` already does. 136 wire pins green, +21 pinned assertions and -0: no pre-existing literal moved, and `response_sites_shape_is_unchanged_by_projects` pins that. Config schema v23 to v24 is a bare bump with `[[projects]]` emitted last and skipped when empty, so all 32 `toml_byte_shape` goldens hold. Round 1 was a REWORK on five judgment findings, the substantive two being a CLI that synthesized `{name}.test` instead of asking the daemon (fixed by an additive `primary_domain` filled from the live router) and a created `orcker.yml` taking PHP from the daemon default while `db`/`preset` were R4 literals (fixed by `DEFAULT_PHP`, and the now-unused parameter removed rather than left dangling); it also demanded coverage for the project-to-proxy projection, added as three `site_domains` tests whose anti-tautology check reverted the `.chain(project_proxies(..))` and watched all three fail. Round 2 was a REWORK on record integrity alone, no code: the AC5 block claimed to be a post-rework re-run while holding the older paste, caught from the code itself because the shipped renderer always emits a `DOMAIN` column and `DEFAULT_PHP` can only write 8.4. AC5's own deviation stands disclosed and was accepted twice: the golden thread was driven at `127.0.0.1:8443` with SNI rather than `https://spike.test` on 443, because the dev instance binds rootless fallback ports and installing the resolver or the privileged bind are sudo changes to the human's machine; TLSv1.3 with SAN `spike.test`, 200 OK and `x-powered-by: PHP/8.4.25` from the SPEC-0005 stack republished on the allocated port. Gate `[gate] OK`; coverage column `—` for the same reason SPEC-0004 recorded, queued as SPEC-0048 | `feat(link): adopt a project directory as a container site on a persistent loopback port` on `feat/SPEC-0006-link-loopback-port` |
| SPEC-0053-validate-container-project-ports | FR-013, FR-021 | 0 | 0 | 17s | — | 1 session (2026-09-03) | APPROVE (round 1) | Closes the three validation holes SPEC-0006 left in `--port`. AC1 is `orckerd tests::link_project_onto_a_bound_listener_is_refused`, a lifecycle test over the real IPC socket: it pins the daemon HTTP listener inside `20000..=29999`, asserts it actually bound, then sends `AddProxy` and `LinkProject` at the same upstream and requires the same `ErrorCode` and a `routing loop` message. Its RED is the spec H1 reproduction run rather than read - the link was accepted on the daemon own bound port 20000, `created: true`. A unit test on the predicate would have passed against the broken build, because the defect was that `Request::LinkProject` never reached the guard at all. AC2 is `orckerd link::tests::port_outside_the_range_is_rejected` (0, 80, FIRST-1, LAST+1 refused with `PortOutOfRange`; FIRST and LAST honored). AC3 is `orcker_config parse::tests::validate_projects_matrix`, six cases over the four R3 invariants. AC2 and AC3 RED as `E0599` on variants that did not exist. R1 reuses the existing guard rather than copying it: the loopback-and-bound-port test moved into `targets_bound_listener`, with `is_self_forward` kept as its URL-parsing front door and `project_loop_refusal` as the project one, so the refusal message has a single source. The guard sits after `plan_link`, not in the dispatch arm, because without `--port` the port is only known once allocated; `AlreadyLinked` is untouched so SPEC-0006 R5 idempotence holds. Three deviations ledgered in `DECISIONS.md`: `validate` rejects a zero port but not an out-of-range one (R4 forbids retroactively rejecting a port SPEC-0006 persisted), three `ValidateErrorReason` variants express R3 four invariants (following the `ProxyNameCollision` precedent the spec names), and R2 makes R1 unreachable on a daemon bound to 80/443. `clippy::too_many_lines` fired at 101/100 on `handle_link_project` and was resolved by extracting `project_loop_refusal`, not by an allow - `scripts/clippy-allow-baseline.txt` is untouched and gate step 5 confirms it. A pre-existing gap found mid-cycle (two variants missing from the `ValidateErrorReason` Display smoke test) was reverted out of the diff and queued as SPEC-0055. AC4 holds with `crates/orcker-config/tests/toml_byte_shape.rs` absent from the diff and 32 goldens green: nothing serialised moved. Gate `[gate] OK`, zero workspace failures; coverage column `—` for the reason SPEC-0004 recorded, queued as SPEC-0048 | `fix(link): validate a container project port before it can claim orcker own listener` on `feat/SPEC-0053-validate-project-ports` |
| SPEC-0049-engine-detection-test-gaps | FR-010 | 0 | 1 | 13s | — | 1 session (2026-09-03) | APPROVE (round 2, after an ESCALATE on the spec's own completeness) | Closes two of SPEC-0004's five untouched areas. AC1 is `orckerd engine_status::tests::an_expired_entry_is_reprobed_and_the_result_is_stored`: `EngineStatusCache::get` now delegates to a private `get_with(probe: impl Future<Output = DockerStatus>)` so a stale entry can be reprobed with `std::future::ready` instead of a live Docker daemon; the test asserts both silent-failure shapes at once (returns the reprobe, not the stale value; `fresh()` then reads the reprobe back). RED was `E0599: no method named get_with`. AC2 is `bin/orcker/tests/cli_e2e.rs::tests::cli_commands_round_trip_against_daemon`, extended to call a new `pub` seam, `orcker::status_outcome(sock, json)`, directly - `run_status` itself is private and prints, so the first attempt hand-replayed its two exchanges instead of calling it, which the supervisor caught: deleting the real `EngineStatus` exchange left that version green. AC2 has no natural RED (the wiring was already correct, only untestable), so mutation evidence was captured instead: production code deliberately broken (`docker`/`note` hardcoded to `(None, None)`), test observed FAILED, reverted, observed green again. Building the real seam surfaced a second-order defect before any test ran: `transport::exchange` resolves the real default daemon socket, not the per-test tempdir one, so the natural extraction would have tested the wrong daemon. Fixed by extending the file's existing `exchange`/`exchange_at` default-path/explicit-path split one level up (`transport::default_sock()`, `status_outcome(sock, json)`), plus a `#[cfg(not(unix))]` `exchange_at` stub so it compiles cross-platform (none existed before). Round 1 ESCALATEd on the spec itself: it reached `approved` and `in_progress` with only a `## Context` section, no `## Requirements` or `## Acceptance checklist` - the ACs being graded existed solely in the implementer's own cycle log, which the supervisor refused to ratify as self-approval (SDD section 4). Fixed jointly with the human, who authored the `draft -> approved` re-transition themselves in a commit separate from the implementation (`7647f8f`, `--numstat` 78 1, ahead of this one; DT9's original approval commit `3bf96a2` also holds). Gate `[gate] OK`, 6/6, `cargo test --workspace` zero `FAILED`; coverage column `—`, same open item as SPEC-0053, queued as SPEC-0048 | `test(engine-status): close the two SPEC-0004 test gaps` on `feat/SPEC-0049-engine-detection-test-gaps` |
| SPEC-0046-elevate-dev-instance-socket | FR-001 | 0 | 1 | 11s | — | 1 session (2026-09-03/04) | APPROVE (round 2, after an ESCALATE on DT9) | Lets `elevate` reach a daemon whose runtime dir is isolated by `docs/developer/building.md`'s XDG dev-instance recipe. AC1 is `elevate::unix_impl::tests::user_socket_candidates_prefers_xdg_runtime_dir_override`: with an XDG override, the first candidate is `{dir}/orcker/orcker.sock`, then the two existing uid-based candidates unchanged, length and order both asserted. AC2 is `..._without_xdg_override_is_unchanged`, table-driven over `None` and `Some("")`, asserting length 2 and the original two paths. RED is `E0061` (arity mismatch): neither test compiles against the pre-fix one-argument `user_socket_candidates`. AC3 is a manual transcript (root required, run by the human): `sudo env XDG_RUNTIME_DIR=... ./target/debug/orcker elevate trust` against a daemon started per the XDG recipe answers `==> trust: ...` / `trusted in 3/3 browser store(s)`, not `daemon not running`. AC4 is `building.md`'s +12 lines documenting `sudo env XDG_RUNTIME_DIR=... orcker elevate` and the silent-wrong-daemon risk of a plain `export` + `sudo`. Round 1 was an ESCALATE, not a REWORK, on DT9: the approval commit bundled the spec's full body (Design & contracts through Agent notes, 105 lines) into the same commit that flipped `draft -> approved`, so the record could not show a human ratifying pre-existing content. Resolved by splitting it into two commits on `main` - `0b81589` (body only, spec kept `draft`) then `9717e61` (status-line-only, `1 1`/`1 1`, matching the `f8a1258`/`eda6cc1` shape) - with `git diff` between the original bundled commit and the new tip empty. Two judgment findings closed in the same round: `assert_eq!(c.len(), ...)` added to both tests (a fourth candidate would previously have passed silently), and `specs/DECISIONS.md` records why R2 is discharged by AC4's narrow scope rather than by fixing the unrelated stale `cargo run -p orckerd -- -v` at `building.md:230`, queued as `SPEC-0056-fix-stale-daemon-serve-subcommand-in-docs` (draft, FR-001). Gate `[gate] OK`, zero workspace failures; coverage column `—` for the reason SPEC-0004 recorded, queued as SPEC-0048 | `fix(elevate): reach a daemon whose runtime dir is XDG-isolated under sudo` on `feat/SPEC-0046-elevate-dev-instance-socket` |
| SPEC-0051-cover-container-link-cli-semantics | FR-021 | 1 | 0 | 17s | — | 1 session (2026-09-05) | APPROVE (round 2, after a REWORK) | Closes the coverage gap SPEC-0006 left on `orcker link`'s container semantics. AC1 is four pure unit tests, `orcker::tests::resolve_link_project_{absolutises_relative_path,omitted_path_uses_cwd,rejects_invalid_name,passes_name_and_port_through}`, against the pre-existing, previously-uncovered `resolve_link_project`; they carried no natural RED (green on first run, since the function already worked), so the supervisor independently reproduced discriminating RED with a mutation sample - dropping `absolutise`, the cwd default, `validate_name` and the `port` passthrough failed all four tests on their own assertion. AC2 required driving `Command::Link` "the way `main` does, not by calling a helper," but the dispatch lived inline inside `run()` with no public seam; extracted verbatim into `pub fn request_for`, the same shape as SPEC-0049's `status_outcome`, with genuine RED (`E0425: cannot find function` before the extraction). AC2's test, `cli_e2e::tests::link_command_creates_then_idempotently_relinks_a_project`, asserts `Response::Project { created: true }` on first link then `created: false` with a byte-identical `orcker.toml` on the idempotent relink. AC3 holds by construction: `git diff` adds only, zero deletions in either the legacy `resolve_link` unit-test block or the `link()` E2E helper. Round 1 was a REWORK on a single finding: the approval commit carried AI-attribution trailers (`Co-Authored-By`, `Claude-Session`), which root `CLAUDE.md` bans outright. Fixed with `git commit --amend` (the commit was still unpushed and only on `main`, verified via `git branch -r --contains` before amending) and the feature branch ref moved onto the corrected commit; round 2 confirmed the trailers gone byte-for-byte and DT9's `--numstat` unchanged (`1 1` / `1 1`), then raised three record-integrity findings the REWORK itself caused - this spec's `attempts` not yet bumped to `1`, no S7 verdict section in the cycle log, and this row not yet existing - all closed in the same commit. Gate `[gate] OK`, 17s; coverage column `—` for the reason SPEC-0004 recorded, queued as SPEC-0048 | `test(link): cover the container-project semantics orcker link ships` on `feat/SPEC-0051-container-link-coverage` |
| SPEC-0030-fix-stale-daemon-run-command-in-docs | FR-001 | 1 | 0 | 14s | — | 1 session (2026-09-05) | APPROVE (round 2, after a REWORK) | Docs-only, no Rust. AC1 is a pattern grep, `grep -cP 'orckerd -- (?!serve)' docs/developer/building.md`, chosen at round 2 to catch the whole defect class rather than the two literal `orckerd -- -v` instances the approved Context named; it read `3` at true `HEAD` RED and `0` after the fix, reproduced independently by the supervisor from `HEAD` blobs both rounds. AC2 (`grep -c 'orckerd -- serve'` -> `3`) and AC3 (all three corrected commands actually boot the daemon on a scratch XDG tree, zero `unexpected argument` errors, RED showed exit 2 twice with two distinct messages) close R1. AC4 (`docs/spike/PHASE0-SPIKE.md`, the historical record of this same defect, untouched) and AC5 (`[gate] OK`) hold both rounds. Round 1 was a REWORK on two findings, both self-inflicted: a third stale block, `orckerd -- --config ~/orcker-dev.toml -v` (`building.md:207`), survived because the round-1 ACs were literal-string greps scoped to the Context's own two examples rather than the requirement's "every"; and a round-1 prose "correction" asserted `serve` is not the default subcommand, which `bin/orckerd/src/main.rs:20` (`unwrap_or_else(|| Command::Serve(ServeArgs::default()))`) refutes - the supervisor independently ran a bare `orckerd --` with no subcommand and confirmed it boots. Both repaired in round 2: the third command fixed, the ACs generalized to the class-catching pattern above, and the prose corrected to the real mechanism (default subcommand, but its own flags need it named, since `Cli`'s parser has no `-v`/`--config` fields to route a bare flag into the default variant). Supersedes `specs/SPEC-0056-fix-stale-daemon-serve-subcommand-in-docs.md`, retracted as a duplicate (`739a3ac`). Gate `[gate] OK`, 14s; coverage column `—`, not applicable (no Rust in the diff) | `docs(developer): fix orckerd's stale run commands in the building guide` on `feat/SPEC-0030-fix-stale-daemon-run-command` |
| SPEC-0048-pure-crate-coverage-metric | FR-001 | 0 | 0 | 11s | 95.91% line / 95.56% region (local run, not yet CI) | 1 session (2026-09-05) | APPROVE (round 1) | Closes the open item every coverage-column `—` since SPEC-0004 pointed at: adds a report-only `Pure-crate coverage (report only)` step to the `rust` job's single `ubuntu-22.04` leg (`if: matrix.os == 'ubuntu-22.04'`, `continue-on-error: true`), running `rustup component add llvm-tools-preview` + `cargo install cargo-llvm-cov --locked` + `cargo llvm-cov --summary-only -p orcker-core -p orcker-stack -p orcker-engine`; `orcker-catalog` omitted from `-p` because it does not exist yet. `scripts/gate.sh` untouched (R2), confirmed by content hash, not name filter. `docs/SDD.md` section 11 gained one English sentence (a Portuguese-native file, per root `CLAUDE.md`'s American-English mandate for every shared record) naming the CI job as the number's source and stating a failed/skipped run leaves the column `—` rather than blocking S8. The spec itself reached `approved` twice: first with only `Context` (missing the sections SDD section 4 requires, the same gap SPEC-0049 was ESCALATEd for), caught before implementation started; the `spec-writer` subagent drafted `Requirements` through `Agent notes` from the approved Context deciding nothing new, then the human re-approved the completed body as a second, separate status-line-only commit. No unit/integration test is possible for a CI-workflow-only change; AC1's evidence is the exact coverage command run locally (real `TOTAL 7631 339 95.56%` / `594 30 94.95%` / `4692 192 95.91%`, region/function/line), since triggering the workflow itself needs a push reserved for the human — reproduced independently by the supervisor, digit for digit. This is the first row able to fill its own `Pure-crate coverage` cell instead of `—`. Gate `[gate] OK`, 11s | `ci(coverage): report pure-crate coverage on the Linux CI leg` on `feat/SPEC-0048-pure-crate-coverage-metric` |
| SPEC-0054-fr-acceptance-must-be-enumerated | FR-001 | 1 | 1 | 14s | — | 1 session (2026-09-05) | APPROVE (round 2, after a REWORK-then-ESCALATE on AC3) | Closes the join DT3 never made: a spec's checklist accounted for its own ACs but never for the PRD ACs of the FR it claimed to `cover`, the exact gap that let SPEC-0005 claim FR-003 with AC1 unwritten. Docs-only, record-only — no Rust, no named tests; every AC is an evidence command. `docs/SDD.md` section 4 gains the `FR acceptance` note (R1/R2, mirrored verbatim in `specs/_TEMPLATE.md`); section 8.1 gains `DT10`, routed to `REWORK` in 8.3/8.4 (R3/R4, mirrored in `.claude/agents/supervisor.md`, which does not read `docs/SDD.md` at runtime); `.claude/agents/spec-writer.md` and `.claude/commands/spec-new.md` require the enumeration at drafting time (R5). AC3's negative reproduction applies `DT10` by hand to `specs/SPEC-0005-proxy-container-spike.md` (FAIL: FR-003 AC1 never enumerated) and `specs/SPEC-0006-link-loopback-port.md` (PASS: FR-003 is not in its `covers`). Round 1 was a REWORK on three findings: `specs/ROADMAP.md`'s Phase-0 header and exit paragraph disagreed on FR-003 (fixed, reworded both to agree); the AC4 grep evidence (`covers`) matched only a pre-existing line in `.claude/commands/spec-new.md` (fixed by wording the new requirement to use the literal word); and AC3's positive control was the substantive one — a full `DT10` audit of SPEC-0006's own covered FRs (FR-021, FR-013) genuinely fails (neither FR-013 AC2 nor FR-021's generation ACs are closed by SPEC-0006 or by SPEC-0053, both being Phase-1/compose-generation concerns that predate any spec), which the agent escalated rather than patch by guessing a replacement control or editing an accepted spec. The human narrowed AC3 to FR-003 only, matching what the spec's own Context section actually discusses; `specs/SPEC-0054-fr-acceptance-must-be-enumerated.md`'s Test plan and AC3 were amended accordingly, recorded in `specs/DECISIONS.md`. `specs/ROADMAP.md`'s stale Phase-0 header (`FR-003 partial`, untrue since SPEC-0006 closed AC1) also corrected, the one line the spec's Agent notes named as riding along for free. Gate `[gate] OK`, 14s | `docs(specs): join a spec's acceptance checklist to the FR acceptance it claims to close` on `feat/SPEC-0054-fr-acceptance-must-be-enumerated` |

> `—` means the value was never recorded by that cycle, not that it is zero. The `Escalates`, `Gate time` and `Pure-crate coverage` columns were added by SPEC-0047, after the tenth accepted spec made the SDD section 11 retrospective due and the record could not answer it. No historical value was inferred: no cycle log stores a `verdict:` block, and deriving escalates from `rounds - attempts` contradicts the prose record (it yields 2 for SPEC-0042, whose log describes three). Cycles from SPEC-0047 onward fill these in at S8.
## Process metrics (reviewed every 10 accepted specs — SDD section 11)

| Window | REWORK rate (target < 30%) | ESCALATE rate (target < 10%) | Median gate time | Pure-crate coverage (target >= 80%) |
|--------|---------------------------|------------------------------|------------------|--------------------------------------|
| — | — | — | — | — |
