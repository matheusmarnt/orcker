[package]
name                   = "orcker-tls"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
publish.workspace      = true
description            = "Local CA and per-site leaf certificates for Orcker."
readme                 = "README.md"

# Internal deps: none. orcker-tls is a leaf in the workspace dep graph.

[dependencies]
pem              = { workspace = true }
rcgen            = { workspace = true }
rustls-pki-types = { workspace = true }
sha2             = { workspace = true }
thiserror        = { workspace = true }
time             = { workspace = true }
x509-parser      = { workspace = true }

[dev-dependencies]
orcker-depcheck = { path = "../orcker-depcheck" }

[lints]
workspace = true
